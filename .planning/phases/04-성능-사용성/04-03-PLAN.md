---
phase: 04-성능-사용성
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Forms/MainForm.Designer.cs
  - Forms/MainForm.cs
  - Forms/AboutForm.cs
autonomous: true
requirements:
  - USAB-01
  - USAB-05
  - USAB-06
  - USAB-07
  - USAB-08

must_haves:
  truths:
    - "모든 툴바 버튼에 마우스를 올리면 한국어 툴팁이 나타난다"
    - "프로그램 상단 제목이 'ANNA 합성데이터 라벨링 툴킷 (ASLT)v1.0'으로 표시된다"
    - "AboutForm 단축키 목록이 Person/Vehicle/Event 섹션으로 구분되고, Vehicle 단축키가 포함되며, event_ 접두어가 제거되어 있다"
    - "USAB-08 수동 추적 기능이 이미 구현되어 동작한다 (추가 작업 불필요)"
  artifacts:
    - path: "Forms/MainForm.Designer.cs"
      provides: "ToolTip 컴포넌트 + labelTitle 정식 명칭"
      contains: "toolTipMain"
    - path: "Forms/MainForm.cs"
      provides: "영상 로드 시 제품명 동적 갱신"
      contains: "ANNA 합성데이터 라벨링 툴킷"
    - path: "Forms/AboutForm.cs"
      provides: "재구성된 단축키 목록 + 정식 제품명"
      contains: "Person 클래스"
  key_links:
    - from: "MainForm.Designer.cs toolTipMain"
      to: "각 버튼 SetToolTip()"
      via: "Designer InitializeComponent"
      pattern: "toolTipMain.SetToolTip"
    - from: "MainForm.Designer.cs labelTitle"
      to: "사용자가 보는 제품명"
      via: "labelTitle.Text"
      pattern: "ANNA 합성데이터 라벨링 툴킷"
---

<objective>
모든 툴바 버튼에 툴팁을 추가하고, 프로그램 제품명을 정식 명칭으로 변경하며, AboutForm 단축키 목록을 클래스별로 재구성한다. USAB-05(Undo/Redo 버튼 활성/비활성)는 버튼이 존재하지 않고 단축키만 있으므로 해당 없음으로 처리한다. USAB-08은 이미 구현 완료.

Purpose: GS인증 사용성(USAB-01, USAB-05, USAB-06, USAB-07, USAB-08) 품질 특성 충족
Output: 툴팁 + 정식 제품명 + 재구성된 단축키 안내
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-성능-사용성/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 툴팁 추가 + 제품명 변경 (USAB-01, USAB-06)</name>
  <files>Forms/MainForm.Designer.cs, Forms/MainForm.cs</files>
  <read_first>Forms/MainForm.Designer.cs, Forms/MainForm.cs</read_first>
  <action>
**A. 툴팁 컴포넌트 추가 (USAB-01) — MainForm.Designer.cs:**

1. InitializeComponent 상단의 컨트롤 생성 영역에 ToolTip 추가:
   ```csharp
   this.toolTipMain = new System.Windows.Forms.ToolTip();
   ```

2. ToolTip 설정:
   ```csharp
   this.toolTipMain.AutoPopDelay = 5000;
   this.toolTipMain.InitialDelay = 500;
   this.toolTipMain.ReshowDelay = 100;
   ```

3. 각 버튼에 SetToolTip 호출 (InitializeComponent 내, 각 버튼 설정 후):
   ```csharp
   this.toolTipMain.SetToolTip(this.btnSelectFolder, "영상 파일 선택 (비디오 열기)");
   this.toolTipMain.SetToolTip(this.btnExportJson, "JSON 저장 (Ctrl+S)");
   this.toolTipMain.SetToolTip(this.btnDeleteJson, "JSON 삭제");
   this.toolTipMain.SetToolTip(this.btnAbout, "프로그램 정보 및 단축키");
   this.toolTipMain.SetToolTip(this.btnMinimize, "최소화");
   this.toolTipMain.SetToolTip(this.btnMaximize, "최대화/복원");
   this.toolTipMain.SetToolTip(this.btnClose, "닫기");
   this.toolTipMain.SetToolTip(this.btnSelectAll, "전체 선택");
   this.toolTipMain.SetToolTip(this.btnEdit, "편집 모드");
   this.toolTipMain.SetToolTip(this.btnPlay, "재생/정지 (Space)");
   this.toolTipMain.SetToolTip(this.btnRewind, "되감기");
   this.toolTipMain.SetToolTip(this.btnForward, "앞으로");
   this.toolTipMain.SetToolTip(this.btnEntry, "Entry 마커 (E)");
   this.toolTipMain.SetToolTip(this.btnExit, "Exit 마커 (X)");
   this.toolTipMain.SetToolTip(this.btnToggleSubtitle, "자막 토글 (C)");
   this.toolTipMain.SetToolTip(this.btnLabelPerson, "Person 라벨 (F1)");
   this.toolTipMain.SetToolTip(this.btnLabelVehicle, "Vehicle 라벨 (F2)");
   this.toolTipMain.SetToolTip(this.btnLabelEvent, "Event 라벨 (F3)");
   this.toolTipMain.SetToolTip(this.btnDeleteLabel, "선택 라벨 삭제 (Delete)");
   this.toolTipMain.SetToolTip(this.btnExportJsonInLabels, "라벨 데이터 JSON 저장");
   ```

4. 필드 선언 영역에 추가:
   ```csharp
   private System.Windows.Forms.ToolTip toolTipMain;
   ```

**B. 제품명 변경 (USAB-06 per D-04, D-05) — MainForm.Designer.cs:**

5. `labelTitle.Text` 변경 (현재 line ~105):
   기존: `this.labelTitle.Text = "ASLT v1.0";`
   변경: `this.labelTitle.Text = "ANNA 합성데이터 라벨링 툴킷 (ASLT)v1.0";`

6. `labelTitle.Font` 크기 축소 (per D-05, 12pt → 10pt):
   기존: `this.labelTitle.Font = new System.Drawing.Font("Segoe UI", 12F, System.Drawing.FontStyle.Bold);`
   변경: `this.labelTitle.Font = new System.Drawing.Font("Segoe UI", 10F, System.Drawing.FontStyle.Bold);`

7. `labelTitle.Size` 확장 (텍스트 길이 증가 대응):
   기존: `this.labelTitle.Size = new System.Drawing.Size(150, 25);`
   변경: `this.labelTitle.Size = new System.Drawing.Size(350, 25);`

**C. 영상 로드 시 제품명 동적 갱신 (USAB-06 per D-06) — MainForm.cs:**

8. MainForm.cs에서 영상 로드 후 labelTitle 갱신하는 라인(line ~279 근처, `$"ASLT v1.0 - {fileName}"` 패턴) 수정:
   기존: `labelTitle.Text = $"ASLT v1.0 - {fileName}";`
   변경: `labelTitle.Text = $"ANNA 합성데이터 라벨링 툴킷 (ASLT)v1.0 - {fileName}";`

9. 빌드 확인: `dotnet build` 오류 0건.
  </action>
  <verify>
    <automated>cd C:/Users/ANNA/AOLTv1.0 && dotnet build --no-restore -c Debug 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ToolTip 컴포넌트 존재: `grep "toolTipMain" Forms/MainForm.Designer.cs` 출력 20줄 이상
    - 모든 주요 버튼에 툴팁 설정: `grep "SetToolTip" Forms/MainForm.Designer.cs` 출력 20줄
    - 정식 제품명: `grep "ANNA 합성데이터 라벨링 툴킷" Forms/MainForm.Designer.cs` 출력 1줄
    - 폰트 10pt: `grep "10F" Forms/MainForm.Designer.cs` 에서 labelTitle Font 확인
    - labelTitle Size 350: `grep "350, 25" Forms/MainForm.Designer.cs` 출력 1줄
    - 동적 갱신: `grep "ANNA 합성데이터 라벨링 툴킷" Forms/MainForm.cs` 출력 1줄 이상
    - `dotnet build` 오류 0건
  </acceptance_criteria>
  <done>모든 툴바 버튼에 한국어 툴팁이 표시되고, 프로그램 제목이 정식 명칭으로 표시됨</done>
</task>

<task type="auto">
  <name>Task 2: 단축키 목록 재구성 + AboutForm 제품명 (USAB-07, USAB-05 해당없음 처리)</name>
  <files>Forms/AboutForm.cs</files>
  <read_first>Forms/AboutForm.cs</read_first>
  <action>
**A. AboutForm 제품명 변경 (USAB-06 per D-05):**

1. lblTitle 텍스트 변경 (line ~31):
   기존: `Text = "ASLT v1.0",`
   변경: `Text = "ANNA 합성데이터 라벨링 툴킷 (ASLT)v1.0",`

2. lblTitle 폰트 크기 조정 (현재 20F → 16F, 긴 제품명에 맞게):
   기존: `Font = new Font("Segoe UI", 20F, FontStyle.Bold),`
   변경: `Font = new Font("Segoe UI", 16F, FontStyle.Bold),`

**B. 단축키 목록 재구성 (USAB-07):**

3. 기존 shortcuts 배열(line ~108-140)을 섹션 구분 + Vehicle 단축키 포함 + event_ 접두어 제거로 전면 교체:

   ```csharp
   var shortcuts = new[]
   {
       // ── 공통 단축키 ──
       ("Space", "재생/정지"),
       ("1", "선택 모드"),
       ("2", "그리기 모드"),
       ("E", "Entry 마커"),
       ("X", "Exit 마커 (웨이포인트 생성)"),
       ("Tab", "박스 순환 선택"),
       ("W / A / S / D", "선택된 박스 이동"),
       ("Delete / G", "선택된 박스 삭제"),
       ("Ctrl+Z", "실행취소"),
       ("Ctrl+Y / Ctrl+Shift+Z", "재실행"),
       ("Ctrl+S", "JSON 내보내기"),
       (", / .", "이전/다음 프레임 (1프레임)"),
       ("\u2190 / \u2192", "이전/다음 프레임 (5초)"),
       ("Shift+\u2190 / Shift+\u2192", "이전/다음 프레임 (2초)"),
       ("Shift+> / Shift+<", "배속 증가/감소"),
       ("C", "자막 토글"),
       ("Escape", "선택 해제"),
       ("", ""),
       // ── Person 클래스 (F1) ──
       ("F1", "Person 클래스 선택"),
       ("F1 + Ctrl+1~0", "Person ID 지정 (1~10)"),
       ("F1 + Alt+1~0", "Person ID 지정 (11~20)"),
       ("", ""),
       // ── Vehicle 클래스 (F2) ──
       ("F2", "Vehicle 클래스 선택"),
       ("F2 + Ctrl+1", "승용차"),
       ("F2 + Ctrl+2", "버스"),
       ("F2 + Ctrl+3", "트럭"),
       ("F2 + Ctrl+4", "오토바이"),
       ("", ""),
       // ── Event 클래스 (F3) ──
       ("F3", "Event 클래스 선택"),
       ("F3 + Ctrl+1", "위험물체"),
       ("F3 + Ctrl+2", "사고"),
       ("F3 + Ctrl+3", "파손"),
       ("F3 + Ctrl+4", "화재"),
       ("F3 + Ctrl+5", "무단침입"),
       ("F3 + Ctrl+6", "누수"),
       ("F3 + Ctrl+7", "고장"),
       ("F3 + Ctrl+8", "분실물"),
       ("F3 + Ctrl+9", "쓰러짐"),
       ("F3 + Ctrl+0", "이상행동"),
   };
   ```

   빈 행 구분자 처리: ListView에 아이템 추가 시 빈 튜플은 구분선 역할 — 기존 foreach 루프에서 빈 문자열 체크 추가:
   ```csharp
   foreach (var (key, desc) in shortcuts)
   {
       if (string.IsNullOrEmpty(key) && string.IsNullOrEmpty(desc))
       {
           var separator = new ListViewItem(" ");
           separator.SubItems.Add(" ");
           separator.BackColor = DarkTheme.Background;
           lvShortcuts.Items.Add(separator);
           continue;
       }
       var item = new ListViewItem(key);
       item.SubItems.Add(desc);
       lvShortcuts.Items.Add(item);
   }
   ```

**C. USAB-05 해당없음 처리:**

4. USAB-05 요구사항은 "Undo/Redo 가능 여부에 따라 버튼이 활성/비활성으로 표시"인데, MainForm.Designer.cs에 btnUndo/btnRedo 버튼이 존재하지 않고 단축키(Ctrl+Z, Ctrl+Y)만 제공됨. 버튼이 없으므로 활성/비활성 시각 표시 대상이 없어 해당 없음 처리. 이 사실을 SUMMARY에 기록.

5. 빌드 확인: `dotnet build` 오류 0건.
  </action>
  <verify>
    <automated>cd C:/Users/ANNA/AOLTv1.0 && dotnet build --no-restore -c Debug 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AboutForm 제품명: `grep "ANNA 합성데이터 라벨링 툴킷" Forms/AboutForm.cs` 출력 1줄
    - Person 섹션 존재: `grep "Person 클래스" Forms/AboutForm.cs` 출력 1줄
    - Vehicle 섹션 존재: `grep "Vehicle 클래스" Forms/AboutForm.cs` 출력 1줄
    - Event 섹션 존재: `grep "Event 클래스" Forms/AboutForm.cs` 출력 1줄
    - Vehicle 단축키 포함: `grep "승용차" Forms/AboutForm.cs` 출력 1줄
    - event_ 접두어 제거됨: `grep "event_" Forms/AboutForm.cs` 출력 0줄
    - 구분선 처리: `grep "separator" Forms/AboutForm.cs` 출력 1줄 이상
    - `dotnet build` 오류 0건
  </acceptance_criteria>
  <done>AboutForm에 정식 제품명이 표시되고, 단축키 목록이 Person/Vehicle/Event 섹션으로 구분되며, event_ 접두어가 제거되고, Vehicle 단축키가 포함됨</done>
</task>

</tasks>

<verification>
1. `dotnet build` 오류 0건
2. `grep "toolTipMain" Forms/MainForm.Designer.cs` — 툴팁 존재 확인
3. `grep "ANNA 합성데이터 라벨링 툴킷" Forms/MainForm.Designer.cs Forms/MainForm.cs Forms/AboutForm.cs` — 제품명 일치 확인
4. `grep "event_" Forms/AboutForm.cs` — event_ 접두어 완전 제거 확인
5. `grep "Vehicle 클래스" Forms/AboutForm.cs` — Vehicle 섹션 존재 확인
</verification>

<success_criteria>
- 모든 툴바 버튼에 한국어 툴팁 표시
- 프로그램 제목 "ANNA 합성데이터 라벨링 툴킷 (ASLT)v1.0" 3곳 일치 (Designer, MainForm 동적, AboutForm)
- 단축키 목록이 공통/Person/Vehicle/Event 4개 섹션으로 구분
- Vehicle 단축키(승용차/버스/트럭/오토바이) 포함
- event_ 접두어 완전 제거 (위험물체, 사고, 파손 등 한국어 표기)
- USAB-05는 버튼 미존재로 해당없음 기록
- USAB-08은 이미 구현 완료로 추가 작업 없음
- 빌드 0 오류
</success_criteria>

<output>
After completion, create `.planning/phases/04-성능-사용성/04-03-SUMMARY.md`
</output>
