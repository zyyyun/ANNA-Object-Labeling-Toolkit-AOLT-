---
phase: 04-성능-사용성
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Forms/MainForm.cs
autonomous: true
requirements:
  - PERF-01
  - MAINT-03

must_haves:
  truths:
    - "프레임 이동 시 바운딩 박스 조회가 딕셔너리 O(1) 룩업으로 수행된다"
    - "hitMargin 등 반복 매직 넘버가 클래스 상수로 추출되어 있다"
    - "기존 Paint 캐시(cachedCurrentFrameBoxes)가 딕셔너리 기반으로 전환되어 동작한다"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "Dictionary<int, List<BoundingBox>> 인덱스 + HIT_MARGIN 상수"
      contains: "_bboxByFrame"
  key_links:
    - from: "Forms/MainForm.cs AddUndoAction/boundingBoxes.Add/Remove"
      to: "InvalidateBoxCache()"
      via: "딕셔너리 캐시 무효화"
      pattern: "_bboxByFrame = null"
    - from: "Forms/MainForm.cs GetBboxesForFrame()"
      to: "_bboxByFrame dictionary"
      via: "O(1) lookup replacing .Where() LINQ"
      pattern: "_bboxByFrame\\[frameIndex\\]"
---

<objective>
프레임별 바운딩 박스 조회를 O(n) LINQ에서 O(1) 딕셔너리 룩업으로 전환하고, 반복 사용되는 매직 넘버를 명명된 상수로 추출한다.

Purpose: GS인증 성능효율성(PERF-01) 및 유지보수성(MAINT-03) 품질 특성 충족
Output: MainForm.cs에 딕셔너리 인덱싱 + 상수 추출 적용
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-성능-사용성/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 딕셔너리 인덱싱 도입 + LINQ Where 조회 대체 (PERF-01)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>Forms/MainForm.cs</read_first>
  <action>
1. Fields 영역에 딕셔너리 필드 추가:
   ```csharp
   private Dictionary<int, List<BoundingBox>> _bboxByFrame = null;
   ```

2. 딕셔너리 빌드 + 조회 헬퍼 메서드 추가 (InvalidateBoxCache 근처):
   ```csharp
   private void RebuildBboxIndex()
   {
       _bboxByFrame = new Dictionary<int, List<BoundingBox>>();
       foreach (var box in boundingBoxes)
       {
           if (!_bboxByFrame.TryGetValue(box.FrameIndex, out var list))
           {
               list = new List<BoundingBox>();
               _bboxByFrame[box.FrameIndex] = list;
           }
           list.Add(box);
       }
   }

   private List<BoundingBox> GetBboxesForFrame(int frameIndex)
   {
       if (_bboxByFrame == null)
           RebuildBboxIndex();
       return _bboxByFrame.TryGetValue(frameIndex, out var list) ? list : new List<BoundingBox>();
   }
   ```

3. `InvalidateBoxCache()` 메서드(현재 line ~2586) 수정:
   기존: `private void InvalidateBoxCache() => lastCachedFrameForPaint = -1;`
   변경:
   ```csharp
   private void InvalidateBoxCache()
   {
       lastCachedFrameForPaint = -1;
       _bboxByFrame = null;
   }
   ```

4. Paint 이벤트의 캐시 빌드 로직(line ~1289-1292) 수정:
   기존: `cachedCurrentFrameBoxes = boundingBoxes.Where(b => b.FrameIndex == currentFrameIndex).ToList();`
   변경: `cachedCurrentFrameBoxes = GetBboxesForFrame(currentFrameIndex);`

5. 나머지 `boundingBoxes.Where(b => b.FrameIndex == ...)` 호출을 `GetBboxesForFrame(...)` 으로 대체.
   대상 라인들 (약 11개소):
   - line ~887: `boundingBoxes.Where(b => b.FrameIndex == entryFrameIndex.Value && b.Label == "person")` → `GetBboxesForFrame(entryFrameIndex.Value).Where(b => b.Label == "person")`
   - line ~888: 동일 패턴 vehicle
   - line ~899-900: exitPersonBoxes/exitVehicleBoxes
   - line ~1653: `var currentFrameBoxes = boundingBoxes.Where(b => b.FrameIndex == currentFrameIndex && !b.IsDeleted).ToList()` → `GetBboxesForFrame(currentFrameIndex).Where(b => !b.IsDeleted).ToList()`
   - line ~1692, ~1708: 동일 패턴
   - line ~2177: event 전파 내 — FrameIndex 범위 조건이므로 이건 boundingBoxes.Where 유지 (범위 조회는 딕셔너리 부적합)
   - line ~2413: 동일 (범위 조회 유지)
   - line ~2637: `GetBboxesForFrame(currentFrameIndex).Where(b => !b.IsDeleted).ToList()`

   주의: `b.FrameIndex == x` 단일 프레임 조회만 GetBboxesForFrame으로 전환. 범위 조건(`b.FrameIndex > x && b.FrameIndex <= y`)은 기존 LINQ 유지.

6. 빌드 확인: `dotnet build` 오류 0건.
  </action>
  <verify>
    <automated>cd C:/Users/ANNA/AOLTv1.0 && dotnet build --no-restore -c Debug 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `_bboxByFrame` 필드가 MainForm.cs에 존재: `grep "_bboxByFrame" Forms/MainForm.cs` 출력 3줄 이상
    - `GetBboxesForFrame` 메서드가 존재: `grep "GetBboxesForFrame" Forms/MainForm.cs` 출력 8줄 이상
    - `RebuildBboxIndex` 메서드가 존재: `grep "RebuildBboxIndex" Forms/MainForm.cs` 출력 2줄 이상
    - InvalidateBoxCache가 _bboxByFrame을 null로 설정: `grep -A2 "InvalidateBoxCache" Forms/MainForm.cs` 에서 `_bboxByFrame = null` 확인
    - 단일 프레임 조회에서 `boundingBoxes.Where(b => b.FrameIndex ==` 패턴이 제거됨 (범위 조건 제외)
    - `dotnet build` 오류 0건
  </acceptance_criteria>
  <done>모든 단일 프레임 bbox 조회가 Dictionary O(1) 룩업을 사용하고, InvalidateBoxCache 호출 시 딕셔너리가 무효화되며, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: 매직 넘버를 명명된 상수로 추출 (MAINT-03)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>Forms/MainForm.cs</read_first>
  <action>
1. 기존 상수 영역(line ~26-29)에 다음 상수 추가:
   ```csharp
   private const int HIT_MARGIN = 4;
   ```

2. `const int hitMargin = 4` 지역 변수(3곳: line ~1654, ~1691, ~1705)를 모두 제거하고 `HIT_MARGIN` 상수로 대체:
   - `hitMargin` → `HIT_MARGIN` (각 사용처에서)

3. 기타 매직 넘버 탐색 및 추출:
   - `HANDLE_SIZE / 2f + 2` (line ~1746)의 `2`는 tolerance offset → 이미 계산식 내에 있으므로 유지 (명확한 의미)
   - Paint 이벤트 내 색상 Alpha 값(200 등)은 시각적 상수이므로 유지
   - `MAX_UNDO_STACK = 100`은 이미 상수 (변경 불필요)

4. 빌드 확인: `dotnet build` 오류 0건.
  </action>
  <verify>
    <automated>cd C:/Users/ANNA/AOLTv1.0 && dotnet build --no-restore -c Debug 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `HIT_MARGIN` 상수가 클래스 필드에 존재: `grep "private const int HIT_MARGIN" Forms/MainForm.cs` 출력 1줄
    - 지역 `const int hitMargin = 4` 가 모두 제거됨: `grep "const int hitMargin" Forms/MainForm.cs` 출력 0줄
    - `HIT_MARGIN` 이 사용되는 곳: `grep "HIT_MARGIN" Forms/MainForm.cs` 출력 4줄 이상 (선언 1 + 사용 3)
    - `dotnet build` 오류 0건
  </acceptance_criteria>
  <done>반복 사용되던 hitMargin 매직 넘버가 클래스 수준 HIT_MARGIN 상수로 추출되고, 빌드 성공</done>
</task>

</tasks>

<verification>
1. `dotnet build` 오류 0건
2. `grep "_bboxByFrame" Forms/MainForm.cs` — 딕셔너리 필드/메서드 존재 확인
3. `grep "const int hitMargin" Forms/MainForm.cs` — 지역 매직 넘버 제거 확인
4. `grep "HIT_MARGIN" Forms/MainForm.cs` — 상수 사용 확인
</verification>

<success_criteria>
- 프레임별 bbox 조회가 Dictionary<int, List<BoundingBox>> 기반 O(1)로 동작
- InvalidateBoxCache() 호출 시 딕셔너리 캐시도 함께 무효화
- hitMargin 매직 넘버가 HIT_MARGIN 클래스 상수로 통합
- 빌드 0 오류, 기존 기능 동작 유지
</success_criteria>

<output>
After completion, create `.planning/phases/04-성능-사용성/04-01-SUMMARY.md`
</output>
