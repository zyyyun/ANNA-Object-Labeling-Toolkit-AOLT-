---
phase: 04-성능-사용성
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Forms/MainForm.cs
  - Services/VideoService.cs
autonomous: true
requirements:
  - USAB-02
  - USAB-04
  - COMP-02

must_haves:
  truths:
    - "bbox를 편집한 후 앱을 닫으면 저장 확인 다이얼로그가 표시된다"
    - "미저장 상태에서 새 영상을 열면 저장 확인 다이얼로그가 표시된다"
    - "FFmpeg 미설치 시 자막 추출 시도 시 사용자에게 안내 메시지가 표시된다"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "_isDirty 플래그 + OnFormClosing 경고 + 영상 전환 확인"
      contains: "_isDirty"
    - path: "Services/VideoService.cs"
      provides: "FFmpeg 미설치 시 사용자 메시지 반환"
      contains: "FFmpeg"
  key_links:
    - from: "AddUndoAction()"
      to: "_isDirty = true"
      via: "모든 편집 조작이 isDirty 설정"
      pattern: "_isDirty = true"
    - from: "SaveCurrentLabelingData()"
      to: "_isDirty = false"
      via: "저장 시 isDirty 초기화"
      pattern: "_isDirty = false"
    - from: "OnFormClosing()"
      to: "MessageBox.Show"
      via: "_isDirty 조건부 경고"
      pattern: "저장하지 않은 변경사항"
---

<objective>
미저장 변경사항 경고 시스템(_isDirty)을 구축하고, 앱 종료/영상 전환 시 확인 다이얼로그를 표시하며, FFmpeg 미설치 시 명확한 안내 메시지를 제공한다.

Purpose: GS인증 사용성(USAB-02, USAB-04) 및 호환성(COMP-02) 품질 특성 충족
Output: isDirty 기반 보호 다이얼로그 + FFmpeg 안내 메시지
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-성능-사용성/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: _isDirty 플래그 + 종료/영상전환 확인 다이얼로그 (USAB-02, USAB-04)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>Forms/MainForm.cs</read_first>
  <action>
1. Fields 영역에 isDirty 플래그 추가:
   ```csharp
   private bool _isDirty = false;
   ```

2. `AddUndoAction` 메서드(line ~2103) 맨 첫줄에 isDirty 설정 추가:
   ```csharp
   private void AddUndoAction(UndoAction action)
   {
       _isDirty = true;
       undoStack.Push(action);
       // ... 나머지 기존 코드
   }
   ```

3. `SaveCurrentLabelingData` 메서드(line ~624) 마지막에 isDirty 초기화 추가:
   메서드 본문 끝(정상 저장 완료 후)에 `_isDirty = false;` 추가.

4. `OnFormClosing` 메서드(line ~2821) 수정 — base.OnFormClosing 호출 전에 isDirty 체크 삽입:
   ```csharp
   protected override void OnFormClosing(FormClosingEventArgs e)
   {
       if (_isDirty && _videoService != null && _videoService.IsVideoLoaded)
       {
           var result = MessageBox.Show(
               "저장하지 않은 변경사항이 있습니다. 저장하시겠습니까?",
               "저장 확인",
               MessageBoxButtons.YesNoCancel,
               MessageBoxIcon.Warning);
           if (result == DialogResult.Yes)
           {
               SaveCurrentLabelingData();
           }
           else if (result == DialogResult.Cancel)
           {
               e.Cancel = true;
               return;
           }
       }
       base.OnFormClosing(e);
       doubleClickTimer?.Dispose();
       doubleClickTimer = null;
       _videoLoadCts?.Cancel();
       _videoLoadCts?.Dispose();
       labelFont?.Dispose();
       _videoService?.Dispose();
   }
   ```

5. 영상 전환 확인 — `btnSelectFolder_Click`(line ~228) 수정:
   OpenFileDialog.ShowDialog() == OK 후, LoadVideoWithSubtitle 호출 전에 isDirty 체크 추가:
   ```csharp
   if (ofd.ShowDialog() == DialogResult.OK)
   {
       if (_isDirty && _videoService != null && _videoService.IsVideoLoaded)
       {
           var result = MessageBox.Show(
               "저장하지 않은 편집이 있습니다. 저장 후 전환하시겠습니까?",
               "저장 확인",
               MessageBoxButtons.YesNoCancel,
               MessageBoxIcon.Warning);
           if (result == DialogResult.Yes)
               SaveCurrentLabelingData();
           else if (result == DialogResult.Cancel)
               return;
       }
       await LoadVideoWithSubtitle(ofd.FileName);
   }
   ```

6. `LoadVideoWithSubtitle` 메서드 시작 부분(line ~243 이후)에서 로드 성공 후 isDirty 초기화:
   영상 로드 성공 분기 내(line ~270 근처, `_videoService.IsVideoLoaded` 확인 후)에 `_isDirty = false;` 추가.

7. 빌드 확인: `dotnet build` 오류 0건.
  </action>
  <verify>
    <automated>cd C:/Users/ANNA/AOLTv1.0 && dotnet build --no-restore -c Debug 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `_isDirty` 필드 존재: `grep "private bool _isDirty" Forms/MainForm.cs` 출력 1줄
    - AddUndoAction에서 isDirty 설정: `grep -A2 "void AddUndoAction" Forms/MainForm.cs` 에 `_isDirty = true` 포함
    - SaveCurrentLabelingData에서 isDirty 초기화: `grep "_isDirty = false" Forms/MainForm.cs` 출력 2줄 이상
    - OnFormClosing에 저장 확인 메시지: `grep "저장하지 않은 변경사항" Forms/MainForm.cs` 출력 1줄
    - 영상 전환 확인 메시지: `grep "저장하지 않은 편집" Forms/MainForm.cs` 출력 1줄
    - Cancel 시 종료 취소: `grep "e.Cancel = true" Forms/MainForm.cs` 출력 1줄
    - `dotnet build` 오류 0건
  </acceptance_criteria>
  <done>bbox 편집 후 앱 종료 또는 영상 전환 시 YesNoCancel 확인 다이얼로그가 표시되고, 저장/취소/무시 동작이 정상 수행됨</done>
</task>

<task type="auto">
  <name>Task 2: FFmpeg 미설치 시 사용자 안내 메시지 (COMP-02)</name>
  <files>Services/VideoService.cs, Forms/MainForm.cs</files>
  <read_first>Services/VideoService.cs, Forms/MainForm.cs</read_first>
  <action>
1. `VideoService.SetupFFmpegPath()` 메서드에서 FFmpeg를 찾지 못했을 때 로그 기록 추가.
   현재 `isFFmpegAvailable = false;` 설정 후 무언 종료 → 로그 추가:
   ```csharp
   // line ~562 근처, isFFmpegAvailable = false 설정 직후:
   Log.Warning("FFmpeg를 찾을 수 없습니다. 자막 추출 기능이 비활성화됩니다. " +
               "PATH에 ffmpeg를 추가하거나 실행 폴더의 ffmpeg/ 하위에 ffmpeg.exe를 배치하세요.");
   ```

2. `VideoService`에 FFmpeg 가용성 public 프로퍼티 추가 (없으면):
   ```csharp
   public bool IsFFmpegAvailable => isFFmpegAvailable;
   ```

3. `VideoService.ExtractSrtFromVideoAsync()` 메서드(line ~389)에서 `!isFFmpegAvailable` early return 전에 로그 추가:
   ```csharp
   if (!isFFmpegAvailable)
   {
       Log.Warning("FFmpeg 미설치로 자막 추출을 건너뜁니다: {VideoPath}", videoPath);
       return false;
   }
   ```

4. `MainForm.cs`에서 `_videoService.SetupFFmpegPath()` 호출(line ~170) 직후에 사용자 알림 추가:
   ```csharp
   _videoService.SetupFFmpegPath();
   if (!_videoService.IsFFmpegAvailable)
   {
       Log.Warning("FFmpeg 미설치 상태로 시작합니다.");
       // 사용자에게 한 번만 안내 (시작 시)
       MessageBox.Show(
           "FFmpeg가 설치되지 않았습니다.\n\n" +
           "자막 추출 기능을 사용하려면 다음 중 하나를 수행하세요:\n" +
           "1. FFmpeg를 설치하고 시스템 PATH에 추가\n" +
           "2. 프로그램 폴더의 ffmpeg/ 하위에 ffmpeg.exe 배치\n\n" +
           "자막 추출 외 기능은 정상적으로 사용할 수 있습니다.",
           "FFmpeg 안내",
           MessageBoxButtons.OK,
           MessageBoxIcon.Information);
   }
   ```

5. 빌드 확인: `dotnet build` 오류 0건.
  </action>
  <verify>
    <automated>cd C:/Users/ANNA/AOLTv1.0 && dotnet build --no-restore -c Debug 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `IsFFmpegAvailable` 프로퍼티 존재: `grep "IsFFmpegAvailable" Services/VideoService.cs` 출력 1줄 이상
    - FFmpeg 미설치 로그: `grep "FFmpeg를 찾을 수 없습니다" Services/VideoService.cs` 출력 1줄
    - 사용자 안내 메시지: `grep "FFmpeg가 설치되지 않았습니다" Forms/MainForm.cs` 출력 1줄
    - `dotnet build` 오류 0건
  </acceptance_criteria>
  <done>FFmpeg 미설치 시 앱 시작 시 사용자에게 안내 MessageBox가 표시되고, 자막 추출 시도 시 로그에 경고가 기록됨</done>
</task>

</tasks>

<verification>
1. `dotnet build` 오류 0건
2. `grep "_isDirty" Forms/MainForm.cs` — isDirty 플래그 시스템 확인
3. `grep "저장하지 않은" Forms/MainForm.cs` — 확인 다이얼로그 메시지 확인
4. `grep "FFmpeg가 설치되지" Forms/MainForm.cs` — FFmpeg 안내 메시지 확인
</verification>

<success_criteria>
- _isDirty 플래그가 AddUndoAction에서 true, SaveCurrentLabelingData에서 false 설정
- 앱 종료 시 미저장 변경사항 경고 다이얼로그 (YesNoCancel)
- 영상 전환 시 미저장 변경사항 확인 다이얼로그 (YesNoCancel)
- FFmpeg 미설치 시 시작 시 안내 MessageBox 표시
- 빌드 0 오류
</success_criteria>

<output>
After completion, create `.planning/phases/04-성능-사용성/04-02-SUMMARY.md`
</output>
