---
phase: 03-기능-정확성-보안
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - Forms/MainForm.cs
autonomous: true
requirements: [FUNC-01, FUNC-05, FUNC-06, FUNC-07, FUNC-08, FUNC-09, FUNC-10]
must_haves:
  truths:
    - "새 영상 로드 시 undoStack, redoStack, entryFrameIndex, exitFrameIndex, currentMode, drawing flags가 초기화된다"
    - "BBOX 삭제가 Delete 키와 삭제 버튼 양쪽에서 boundingBoxes.Remove()로 통일된다"
    - "Vehicle ComboBox 선택이 정확한 차량 종류를 반영한다"
    - "단축키 ID 변경이 선택된 box의 waypoint 범위 내에서만 동작한다"
    - "vehicle Ctrl+1~4와 event Ctrl+1~10 단축키가 충돌 없이 동작한다"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "상태 초기화, 통일된 삭제, 수정된 단축키 핸들러"
      contains: "undoStack.Clear"
  key_links:
    - from: "Forms/MainForm.cs (LoadLabelingData)"
      to: "undoStack, redoStack, entryFrameIndex, exitFrameIndex"
      via: "Clear/null 초기화"
      pattern: "undoStack\\.Clear"
    - from: "Forms/MainForm.cs (Delete key handler)"
      to: "boundingBoxes.Remove"
      via: "IsDeleted 대신 Remove 사용"
      pattern: "boundingBoxes\\.Remove\\(selectedBox\\)"
---

<objective>
MainForm의 핵심 기능 버그 7건을 수정한다: 상태 초기화, 삭제 통일, Vehicle 드롭다운, 단축키 일관성.

Purpose: GS인증 기능적합성 평가에서 핵심 조작 시나리오 결함 제거
Output: 영상 전환/삭제/단축키가 정확하게 동작하는 MainForm
</objective>

<execution_context>
@C:\Users\ANNA\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ANNA\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-기능-정확성-보안/03-01-SUMMARY.md
@Forms/MainForm.cs
</context>

<interfaces>
<!-- MainForm fields that need reset (from line 51-95) -->
```csharp
private int currentAssignedId = 1;             // line 51
private DrawMode currentMode = DrawMode.Select; // line 59
private bool isDrawing;                         // line 60
private bool isDragging;                        // line 61
private bool isResizing;                        // line 62
private int? entryFrameIndex;                   // line 71
private int? exitFrameIndex;                    // line 72
private Stack<UndoAction> undoStack;            // line 94
private Stack<UndoAction> redoStack;            // line 95
```

<!-- Delete key handler: line 2152-2156 -->
<!-- btnDeleteLabel_Click: line 2310-2317 -->
<!-- Vehicle ComboBox: line 2458-2493 -->
<!-- KeyDown shortcut handlers: line 2057-2126 -->
<!-- ChangeBoxIdWithinWaypoint: line 2202-2218 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 영상 전환 시 상태 완전 초기화 + BBOX 삭제 통일</name>
  <files>Forms/MainForm.cs</files>
  <read_first>Forms/MainForm.cs (lines 415-456 LoadLabelingData, lines 2152-2156 Delete key, lines 2310-2317 btnDeleteLabel_Click)</read_first>
  <action>
1. FUNC-09: 새 영상 로드 시 상태 완전 초기화.
MainForm.cs의 LoadLabelingData 메서드 (line 422-430 근처), 기존 초기화 코드 (`boundingBoxes.Clear()` 등) 바로 뒤에 누락된 상태 초기화 추가:
```csharp
// 새 영상 로드 시 이전 라벨링 상태를 항상 초기화
boundingBoxes.Clear();
waypointMarkers.Clear();
selectedBox = null;
selectedWaypoint = null;
categoryMap = new Dictionary<int, CategoryData>();
frameTimestampMap = new Dictionary<int, string>();
nextAnnotationId = 1;
labelCurrentJsonFile.Text = "";

// FUNC-09: 이전 작업 상태 완전 초기화
undoStack.Clear();
redoStack.Clear();
entryFrameIndex = null;
exitFrameIndex = null;
currentMode = DrawMode.Select;
currentAssignedId = 1;
isDrawing = false;
isDragging = false;
isResizing = false;
```

2. FUNC-10: BBOX 삭제를 boundingBoxes.Remove()로 통일.
Delete 키 핸들러 (line 2152-2156) 변경:
BEFORE:
```csharp
else if ((e.KeyCode == Keys.Delete || e.KeyCode == Keys.G) && selectedBox != null)
{
    AddUndoAction(new UndoAction { Type = UndoActionType.RemoveBox, Box = CloneBoundingBox(selectedBox) });
    selectedBox.IsDeleted = true;
    selectedBox = null; UpdateBoxCount(); UpdateBboxListDisplay(); pictureBoxVideo.Invalidate(); e.Handled = true;
}
```
AFTER:
```csharp
else if ((e.KeyCode == Keys.Delete || e.KeyCode == Keys.G) && selectedBox != null)
{
    AddUndoAction(new UndoAction { Type = UndoActionType.RemoveBox, Box = CloneBoundingBox(selectedBox) });
    boundingBoxes.Remove(selectedBox);
    selectedBox = null;
    InvalidateBoxCache(); UpdateBoxCount(); UpdateBboxListDisplay(); pictureBoxVideo.Invalidate(); e.Handled = true;
}
```
핵심 변경: `selectedBox.IsDeleted = true` -> `boundingBoxes.Remove(selectedBox)` + `InvalidateBoxCache()` 추가.
이렇게 하면 btnDeleteLabel_Click (line 2314)과 동일한 삭제 경로를 사용하여 내부 상태 일치 보장.

3. FUNC-01/FUNC-05: Vehicle ComboBox 선택 검증.
Vehicle ComboBox (line 2458-2493) 분석: `GetCategoryName()`이 반환하는 값과 ComboBox options 배열의 값이 일치해야 한다. line 2475에서 `text = GetCategoryName(capturedBox.Label, GetBoxId(capturedBox))`이고 options는 `{"car", "motorcycle", "e_scooter", "bicycle"}`. GetCategoryName에서 vehicle은 `vehicleTypeNames` 딕셔너리를 사용하는데, 이 딕셔너리의 값이 ComboBox options와 정확히 일치하는지 확인한다.

JsonService.cs의 GetCategoryName (line 69 이후)을 읽어 vehicle 매핑 확인. 만약 불일치가 있으면 ComboBox options 또는 GetCategoryName을 일치시킨다.
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "undoStack.Clear" Forms/MainForm.cs returns >= 1
    - grep -c "redoStack.Clear" Forms/MainForm.cs returns >= 1
    - grep -c "entryFrameIndex = null" Forms/MainForm.cs returns >= 2 (reset + Escape)
    - grep "Delete.*Keys.G" Forms/MainForm.cs shows boundingBoxes.Remove (not IsDeleted = true)
    - grep -c "InvalidateBoxCache" Forms/MainForm.cs shows it is called in Delete key handler
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>새 영상 로드 시 모든 상태가 초기화되고, BBOX 삭제가 Remove()로 통일되어 내부 데이터와 화면 상태가 일치한다</done>
</task>

<task type="auto">
  <name>Task 2: 단축키 충돌 해소 + ID 변경 범위 검증</name>
  <files>Forms/MainForm.cs</files>
  <read_first>Forms/MainForm.cs (lines 2057-2126 KeyDown shortcuts, lines 2202-2218 ChangeBoxIdWithinWaypoint)</read_first>
  <action>
1. FUNC-07: Vehicle/Event 단축키 충돌 해소.
현재 코드 (line 2101-2126):
- Vehicle block (2101): `e.Control && (currentSelectedLabel == "vehicle" || (selectedBox != null && selectedBox.Label == "vehicle"))`
- Event block (2115): `e.Control && (currentSelectedLabel == "event" || (selectedBox != null && selectedBox.Label == "event"))`

두 블록 모두 `e.Control && !e.Shift && !e.Alt`를 검사하며, Ctrl+1~4 범위에서 충돌할 수 있다.
예: currentSelectedLabel == "event"이고 selectedBox.Label == "vehicle"이면 vehicle 블록이 먼저 실행되어 vehicle ID가 변경됨 (이것은 의도된 동작일 수 있음).

수정: vehicle 블록 조건을 selectedBox 우선으로 변경하여 명확하게 만든다:
```csharp
// Ctrl+1~4: vehicle 클래스 선택
if (e.Control && !e.Shift && !e.Alt && selectedBox != null && selectedBox.Label == "vehicle")
{
    int? classId = GetIdFromKey(e.KeyCode, 0);
    if (classId.HasValue && classId.Value >= 1 && classId.Value <= 4)
    {
        ChangeBoxIdWithinWaypoint(selectedBox, classId.Value);
        e.Handled = true; return;
    }
}
// Ctrl+1~4: vehicle 기본 ID 지정 (선택된 box 없을 때)
else if (e.Control && !e.Shift && !e.Alt && currentSelectedLabel == "vehicle" && selectedBox == null)
{
    int? classId = GetIdFromKey(e.KeyCode, 0);
    if (classId.HasValue && classId.Value >= 1 && classId.Value <= 4)
    {
        currentAssignedId = classId.Value;
        e.Handled = true; return;
    }
}

// Ctrl+1~0: event 클래스 선택
if (e.Control && !e.Shift && !e.Alt && selectedBox != null && selectedBox.Label == "event")
{
    int? classId = GetIdFromKey(e.KeyCode, 0);
    if (classId.HasValue && classId.Value >= 1 && classId.Value <= 10)
    {
        ChangeBoxIdWithinWaypoint(selectedBox, classId.Value);
        e.Handled = true; return;
    }
}
else if (e.Control && !e.Shift && !e.Alt && currentSelectedLabel == "event" && selectedBox == null)
{
    int? classId = GetIdFromKey(e.KeyCode, 0);
    if (classId.HasValue && classId.Value >= 1 && classId.Value <= 10)
    {
        currentAssignedId = classId.Value;
        e.Handled = true; return;
    }
}
```
핵심: selectedBox가 있으면 selectedBox.Label 기준으로만 판단. selectedBox가 없으면 currentSelectedLabel 기준. 이렇게 하면 vehicle box가 선택된 상태에서 event 단축키가 발동하는 일이 없다.

2. FUNC-08: 다중 BBOX에서 선택 객체만 변경 검증.
ChangeBoxIdWithinWaypoint (line 2202-2218)의 동작 확인:
- waypoint가 있으면: waypoint 범위 내 동일 label+oldId 가진 box들 변경 (이것은 정상 -- waypoint는 하나의 객체 트랙이므로)
- waypoint가 없으면: selectedBox만 변경 (line 2213-2216, 이것도 정상)
이 로직은 FUNC-08 요구사항을 이미 충족한다. 다만, selectedBox가 특정 waypoint에 속하지만 같은 프레임에 같은 label+ID의 다른 box가 다른 waypoint에 있는 경우를 확인:
- `b.FrameIndex >= waypoint.EntryFrame && b.FrameIndex <= waypoint.ExitFrame` 조건이 다른 waypoint의 box를 포함할 수 있음
- 추가 조건: `FindWaypointForBox(b) == waypoint` 검증 추가 고려

수정: ChangeBoxIdWithinWaypoint에서 boxesToUpdate 필터에 waypoint 일치 검증 추가:
```csharp
if (waypoint != null)
{
    var boxesToUpdate = boundingBoxes.Where(b =>
        b.Label == box.Label &&
        GetBoxId(b) == oldId &&
        b.FrameIndex >= waypoint.EntryFrame &&
        b.FrameIndex <= waypoint.ExitFrame &&
        !b.IsDeleted &&
        FindWaypointForBox(b) == waypoint  // 같은 waypoint에 속한 box만
    ).ToList();
    foreach (var b in boxesToUpdate) SetBoxId(b, box.Label, newId);
    waypoint.ObjectId = newId;
    UpdateWaypointListView();
}
```

3. FUNC-06: ID 사후 지정 시 waypoint ObjectId 동기화.
ChangeBoxIdWithinWaypoint는 이미 `waypoint.ObjectId = newId` (line 2210)를 설정하므로 정상. 단, FindWaypointForBox가 oldId 기준으로 매칭하는 경우 문제 없는지 확인. FindWaypointForBox는 box의 FrameIndex와 Label로 waypoint를 찾으므로 ID 변경 전에 호출되면 문제없다.
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "selectedBox != null && selectedBox.Label == .vehicle." Forms/MainForm.cs shows vehicle shortcut checks selectedBox.Label
    - grep "selectedBox != null && selectedBox.Label == .event." Forms/MainForm.cs shows event shortcut checks selectedBox.Label
    - grep "FindWaypointForBox(b) == waypoint" Forms/MainForm.cs shows waypoint scope filter in ChangeBoxIdWithinWaypoint
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Vehicle/Event 단축키가 selectedBox.Label 기준으로 충돌 없이 동작하고, ID 변경이 선택된 box의 waypoint 범위 내에서만 적용된다</done>
</task>

<task type="auto">
  <name>Task 3: MainForm bbox 조작 시 클램핑 적용</name>
  <files>Forms/MainForm.cs</files>
  <read_first>Forms/MainForm.cs (lines 1373-1450 MouseMove/MouseUp, lines 1608-1652 PerformResize, lines 2145-2150 WASD move)</read_first>
  <action>
Plan 01에서 CoordinateHelper.ClampToImage를 생성했으므로, MainForm의 모든 bbox 좌표 설정 경로에 클램핑을 적용한다 (per FUNC-03).

1. MouseUp에서 drawing 완료 시 (line 1428-1437):
기존: `boundingBoxes.Add(drawingBox);`
수정: drawingBox 추가 전에 클램핑:
```csharp
if (drawingBox.Rectangle.Width > 10 && drawingBox.Rectangle.Height > 10)
{
    // FUNC-03: 이미지 범위 클램핑
    if (pictureBoxVideo.Image != null)
        drawingBox.Rectangle = CoordinateHelper.ClampToImage(drawingBox.Rectangle, pictureBoxVideo.Image.Width, pictureBoxVideo.Image.Height);
    boundingBoxes.Add(drawingBox);
```

2. MouseMove에서 drag 완료 시 (line 1403-1408):
```csharp
else if (isDragging && selectedBox != null)
{
    var viewPos = new PointF(e.X - dragOffset.X, e.Y - dragOffset.Y);
    var imagePos = CoordinateHelper.ViewToImage(viewPos, pictureBoxVideo);
    selectedBox.Rectangle = new Rectangle((int)imagePos.X, (int)imagePos.Y, selectedBox.Rectangle.Width, selectedBox.Rectangle.Height);
    // FUNC-03: 이미지 범위 클램핑
    if (pictureBoxVideo.Image != null)
        selectedBox.Rectangle = CoordinateHelper.ClampToImage(selectedBox.Rectangle, pictureBoxVideo.Image.Width, pictureBoxVideo.Image.Height);
    pictureBoxVideo.Invalidate();
}
```

3. PerformResize 완료 시 (line 1651):
```csharp
selectedBox.Rectangle = newRect;
// FUNC-03: 이미지 범위 클램핑
if (pictureBoxVideo.Image != null)
    selectedBox.Rectangle = CoordinateHelper.ClampToImage(selectedBox.Rectangle, pictureBoxVideo.Image.Width, pictureBoxVideo.Image.Height);
```

4. WASD 키보드 이동 (line 2149-2150):
```csharp
switch (e.KeyCode) { case Keys.W: rect.Y -= moveAmount; break; case Keys.A: rect.X -= moveAmount; break; case Keys.S: rect.Y += moveAmount; break; case Keys.D: rect.X += moveAmount; break; }
// FUNC-03: 이미지 범위 클램핑
if (pictureBoxVideo.Image != null)
    rect = CoordinateHelper.ClampToImage(rect, pictureBoxVideo.Image.Width, pictureBoxVideo.Image.Height);
selectedBox.Rectangle = rect; pictureBoxVideo.Invalidate(); e.Handled = true;
```
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "CoordinateHelper.ClampToImage" Forms/MainForm.cs returns >= 4 (draw, drag, resize, keyboard)
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Drawing, dragging, resizing, keyboard 이동 모든 경로에서 bbox 좌표가 이미지 범위 내로 클램핑된다</done>
</task>

</tasks>

<verification>
- `dotnet build -c Release --no-incremental` 성공
- 새 영상 로드 시 undoStack/redoStack/entry/exit가 초기화됨
- Delete 키와 삭제 버튼이 동일한 Remove() 경로 사용
- Vehicle/Event 단축키가 selectedBox.Label 기준으로 분리
- bbox 조작 4개 경로에 ClampToImage 적용
</verification>

<success_criteria>
- 영상 전환 시 이전 작업 상태가 완전히 초기화됨
- BBOX 삭제가 내부 데이터에 즉시 반영됨
- 단축키가 선택된 box의 label에 따라 정확하게 동작
- bbox 좌표가 모든 조작 경로에서 이미지 범위 내로 클램핑
- 빌드 성공, 컴파일 오류 0건
</success_criteria>

<output>
After completion, create `.planning/phases/03-기능-정확성-보안/03-03-SUMMARY.md`
</output>
