---
phase: 03-기능-정확성-보안
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/CoordinateHelper.cs
  - Services/JsonService.cs
autonomous: true
requirements: [FUNC-02, FUNC-03, FUNC-04, COMP-01]
must_haves:
  truths:
    - "내보낸 JSON의 타임스탬프가 프레임 상대 시간 기반이며 DateTime.Now를 사용하지 않는다"
    - "바운딩 박스 좌표가 이미지 범위(0~frameWidth, 0~frameHeight)를 절대 초과하지 않는다"
    - "COCO JSON의 카테고리 ID가 person(1-20), vehicle(21-24), event(25-34) 범위를 정확히 매핑한다"
    - "내보낸 JSON이 [x, y, w, h] 형식의 유효한 bbox를 포함하여 타 ML 도구와 호환된다"
  artifacts:
    - path: "Helpers/CoordinateHelper.cs"
      provides: "ClampToImage static method"
      contains: "ClampToImage"
    - path: "Services/JsonService.cs"
      provides: "TimeSpan 기반 타임스탬프, 클램핑 적용된 bbox export"
      contains: "TimeSpan.FromSeconds"
  key_links:
    - from: "Services/JsonService.cs"
      to: "Helpers/CoordinateHelper.cs"
      via: "ClampToImage call in ExportToJsonExtended"
      pattern: "CoordinateHelper\\.ClampToImage"
---

<objective>
COCO JSON 내보내기의 타임스탬프, bbox 좌표, 카테고리 ID 정확성을 수정한다.

Purpose: GS인증 기능적합성 평가에서 JSON 출력 정합성 결함 제거
Output: 정확한 타임스탬프, 클램핑된 좌표, 올바른 카테고리 매핑을 가진 COCO JSON 출력
</objective>

<execution_context>
@C:\Users\ANNA\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ANNA\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Helpers/CoordinateHelper.cs
@Services/JsonService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClampToImage 헬퍼 추가 + JSON 타임스탬프 수정</name>
  <files>Helpers/CoordinateHelper.cs, Services/JsonService.cs</files>
  <read_first>Helpers/CoordinateHelper.cs, Services/JsonService.cs (lines 406-560)</read_first>
  <action>
1. CoordinateHelper.cs에 ClampToImage static method 추가 (per FUNC-03):
```csharp
/// <summary>
/// 바운딩 박스 좌표를 이미지 범위 내로 클램핑합니다.
/// </summary>
public static Rectangle ClampToImage(Rectangle rect, int imageWidth, int imageHeight)
{
    int x = Math.Max(0, Math.Min(rect.X, imageWidth - 1));
    int y = Math.Max(0, Math.Min(rect.Y, imageHeight - 1));
    int w = Math.Min(rect.Width, imageWidth - x);
    int h = Math.Min(rect.Height, imageHeight - y);
    return new Rectangle(x, y, Math.Max(1, w), Math.Max(1, h));
}
```

2. JsonService.cs ExportToJsonExtended (line 428-432) 타임스탬프 수정 (per FUNC-02):
BEFORE (line 428-432):
```csharp
double frameSeconds = frameGroup.Key / fps;
DateTime frameTime = DateTime.Now.AddSeconds(frameSeconds);
string? subtitleTimestamp = videoService?.GetSubtitleTimestampForFrame(frameGroup.Key);
string timestamp = subtitleTimestamp ?? frameTime.ToString("yyyy-MM-ddTHH:mm:ss.fff");
```
AFTER:
```csharp
double frameSeconds = frameGroup.Key / fps;
TimeSpan frameTimeSpan = TimeSpan.FromSeconds(frameSeconds);
string? subtitleTimestamp = videoService?.GetSubtitleTimestampForFrame(frameGroup.Key);
string timestamp = subtitleTimestamp ?? frameTimeSpan.ToString(@"hh\:mm\:ss\.fff");
```

3. 동일 패턴을 entry/exit 타임스탬프에도 적용 (line 492-495):
BEFORE:
```csharp
double entrySeconds = entryFrame / fps;
double exitSeconds = exitFrame / fps;
DateTime entryTime = DateTime.Now.AddSeconds(entrySeconds);
DateTime exitTime = DateTime.Now.AddSeconds(exitSeconds);
```
AFTER:
```csharp
double entrySeconds = entryFrame / fps;
double exitSeconds = exitFrame / fps;
TimeSpan entryTimeSpan = TimeSpan.FromSeconds(entrySeconds);
TimeSpan exitTimeSpan = TimeSpan.FromSeconds(exitSeconds);
```
그리고 이 변수를 사용하는 하위 코드에서 `entryTime.ToString(...)` / `exitTime.ToString(...)`을 `entryTimeSpan.ToString(@"hh\:mm\:ss\.fff")` / `exitTimeSpan.ToString(@"hh\:mm\:ss\.fff")`으로 교체.

4. ExportToJsonExtended에서 bbox 좌표 클램핑 적용 (per FUNC-03, COMP-01):
bbox 배열 생성 부분 (line 502 근처):
```csharp
Bbox = new int[] { box.Rectangle.X, box.Rectangle.Y, box.Rectangle.Width, box.Rectangle.Height },
```
를 다음으로 교체:
```csharp
var clampedRect = CoordinateHelper.ClampToImage(box.Rectangle, frameWidth, frameHeight);
Bbox = new int[] { clampedRect.X, clampedRect.Y, clampedRect.Width, clampedRect.Height },
Area = clampedRect.Width * clampedRect.Height,
```
(기존 Area 계산도 clamped 값 사용하도록 교체)

5. JsonService.cs 상단에 `using ASLTv1.Helpers;` 추가 (CoordinateHelper 접근용).

6. GetCategoryId fallback 분기 검증 (per FUNC-04): line 62-66의 fallback 로직이 범위를 정확히 매핑하는지 확인. 현재 `Math.Min(boxId, 20)`은 person boxId가 20 초과 시 20으로 클램핑. vehicle은 `Math.Min(21 + (boxId - 1), 24)` = boxId 4 초과 시 24로 클램핑. 이 로직은 정상이므로 유지. 단, boxId가 0 이하인 경우를 방어:
```csharp
if (label == "person") return Math.Clamp(boxId, 1, 20);
if (label == "vehicle") return Math.Clamp(21 + (boxId - 1), 21, 24);
if (label == "event") return Math.Clamp(25 + (boxId - 1), 25, 34);
```
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ClampToImage" Helpers/CoordinateHelper.cs returns >= 1
    - grep -c "TimeSpan.FromSeconds" Services/JsonService.cs returns >= 2
    - grep -c "DateTime.Now.AddSeconds" Services/JsonService.cs returns 0
    - grep -c "CoordinateHelper.ClampToImage" Services/JsonService.cs returns >= 1
    - grep -c "Math.Clamp" Services/JsonService.cs returns >= 3
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>JSON 타임스탬프가 프레임 상대 시간 기반이고, bbox 좌표가 이미지 범위 내로 클램핑되며, 카테고리 ID fallback이 범위를 정확히 매핑한다</done>
</task>

<task type="auto">
  <name>Task 2: JSON 로드 시 bbox 클램핑 적용</name>
  <files>Services/JsonService.cs</files>
  <read_first>Services/JsonService.cs (lines 159-397, LoadLabelingDataAsync)</read_first>
  <action>
JsonService.LoadLabelingDataAsync에서 JSON 파일로부터 로드된 bbox 좌표에 클램핑을 적용한다 (per FUNC-03, COMP-01).

LoadLabelingDataAsync 내부에서 BoundingBox를 result.BoundingBoxes에 추가하는 부분을 찾아, 각 bbox의 Rectangle에 ClampToImage를 적용:

로드 후 클램핑:
```csharp
// 로드된 bbox에 대해 이미지 범위 클램핑 적용
foreach (var box in result.BoundingBoxes)
{
    box.Rectangle = CoordinateHelper.ClampToImage(box.Rectangle, frameWidth, frameHeight);
}
```

이 코드를 `result.Success = true;` 직전 (line 380 근처)에 삽입한다. `frameWidth`와 `frameHeight`는 메서드 파라미터 `fps`와 함께 전달받아야 하므로, LoadLabelingDataAsync 시그니처에 `int frameWidth = 0, int frameHeight = 0` 옵셔널 파라미터 추가.

단, 시그니처 변경이 호출부(MainForm.cs line 415)에 영향을 주므로 MainForm.cs의 호출부도 업데이트:
```csharp
var result = await _jsonService.LoadLabelingDataAsync(videoFilePath, _videoService.Fps, _videoService.FrameWidth, _videoService.FrameHeight);
```

frameWidth/frameHeight가 0인 경우(이전 호환성) 클램핑을 건너뛰도록 조건 추가:
```csharp
if (frameWidth > 0 && frameHeight > 0)
{
    foreach (var box in result.BoundingBoxes)
    {
        box.Rectangle = CoordinateHelper.ClampToImage(box.Rectangle, frameWidth, frameHeight);
    }
}
```
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ClampToImage" Services/JsonService.cs returns >= 2 (export + load)
    - grep -c "frameWidth" Services/JsonService.cs (LoadLabelingDataAsync signature) shows parameter exists
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>JSON 로드 시에도 bbox 좌표가 이미지 범위 내로 클램핑된다</done>
</task>

</tasks>

<verification>
- `dotnet build -c Release --no-incremental` 성공
- JsonService.cs에 DateTime.Now.AddSeconds 호출이 없음
- CoordinateHelper.cs에 ClampToImage 메서드 존재
- GetCategoryId에서 Math.Clamp 사용
</verification>

<success_criteria>
- COCO JSON 타임스탬프가 프레임 상대 시간(HH:mm:ss.fff) 형식
- bbox 좌표가 export/load 양방향에서 이미지 범위 클램핑
- 카테고리 ID fallback이 각 라벨 범위를 정확히 클램핑
- 빌드 성공, 컴파일 오류 0건
</success_criteria>

<output>
After completion, create `.planning/phases/03-기능-정확성-보안/03-01-SUMMARY.md`
</output>
