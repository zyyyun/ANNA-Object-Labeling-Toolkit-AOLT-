---
phase: 03-기능-정확성-보안
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - Services/JsonService.cs
  - Services/VideoService.cs
  - Forms/MainForm.cs
autonomous: true
requirements: [RELI-05, USAB-03, MAINT-02]
must_haves:
  truths:
    - "손상된 JSON 파일 로드 시 JsonReaderException이 사용자 친화적 메시지로 표시된다"
    - "손상된 SRT 파일 로드 시 FormatException이 처리되어 크래시가 발생하지 않는다"
    - "모든 오류 메시지가 문제 설명 + 해결 방법을 포함한다"
    - "generic catch(Exception)이 구체적 예외 타입 처리 후 fallback으로만 남는다"
  artifacts:
    - path: "Services/JsonService.cs"
      provides: "JsonReaderException, JsonSerializationException 구체적 처리"
      contains: "JsonReaderException"
    - path: "Services/VideoService.cs"
      provides: "FormatException, IOException 구체적 처리"
      contains: "FormatException"
    - path: "Forms/MainForm.cs"
      provides: "구체적 예외 타입 + 사용자 친화적 메시지"
      contains: "IOException"
  key_links:
    - from: "Services/JsonService.cs (LoadLabelingDataAsync)"
      to: "사용자 메시지"
      via: "JsonReaderException catch -> 줄/열 위치 포함 메시지"
      pattern: "jrEx\\.LineNumber"
    - from: "Services/VideoService.cs (LoadSrtFileAsync)"
      to: "FormatException catch"
      via: "ParseSrtTime FormatException handling"
      pattern: "FormatException"
---

<objective>
손상 파일 크래시 방지, 사용자 친화적 오류 메시지, generic catch(Exception) 구체화를 수행한다.

Purpose: GS인증 신뢰성/사용성/유지보수성 평가에서 예외 처리 품질 기준 충족
Output: 구체적 예외 처리와 사용자 안내 메시지가 적용된 3개 파일
</objective>

<execution_context>
@C:\Users\ANNA\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ANNA\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Services/JsonService.cs
@Services/VideoService.cs
@Forms/MainForm.cs
</context>

<interfaces>
<!-- catch(Exception) locations to update -->
From Services/JsonService.cs:
```csharp
line 206: catch (Exception backupEx) — backup creation failure
line 388: catch (Exception ex) — LoadLabelingDataAsync main catch
line 559: catch (Exception ex) — ExportToJsonExtended
```

From Services/VideoService.cs:
```csharp
line 211: catch (Exception ex) — LoadFrame
line 475: catch (Exception ex) — LoadSrtFileAsync
```

From Forms/MainForm.cs:
```csharp
line 297: catch (Exception ex) — LoadVideoWithSubtitle
line 347: catch (Exception ex) — LoadFrame
line 453: catch (Exception ex) — LoadLabelingData
line 519: catch (Exception ex) — btnExportJson inner
line 525: catch (Exception outerEx) — btnExportJson outer
line 628: catch (Exception ex) — btnPlay
line 694: catch (Exception ex) — btnExit
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: JsonService + VideoService 구체적 예외 처리</name>
  <files>Services/JsonService.cs, Services/VideoService.cs</files>
  <read_first>Services/JsonService.cs (lines 195-397), Services/VideoService.cs (lines 180-216, 460-498)</read_first>
  <action>
1. RELI-05 + MAINT-02: JsonService.LoadLabelingDataAsync (line 383-394) 예외 처리 구체화.
기존 `catch (OutOfMemoryException)` 유지하고, 그 아래의 `catch (Exception ex)`를 구체적 타입으로 분리:

```csharp
catch (OutOfMemoryException oomEx)
{
    result.Success = false;
    result.ErrorMessage = $"메모리 부족으로 파일을 로드할 수 없습니다.\n\n" +
        $"해결 방법: 다른 프로그램을 종료하고 다시 시도하세요.\n" +
        $"상세: {oomEx.Message}";
}
catch (Newtonsoft.Json.JsonReaderException jrEx)
{
    result.Success = false;
    result.ErrorMessage = $"JSON 파일이 손상되었습니다.\n\n" +
        $"파일: {Path.GetFileName(loadPath)}\n" +
        $"위치: 줄 {jrEx.LineNumber}, 열 {jrEx.LinePosition}\n\n" +
        $"해결 방법: 백업 파일({Path.GetFileName(loadPath)}.backup)을 확인하거나 " +
        $"새로 라벨링을 시작하세요.";
    Log.Warning("[JSON 파싱 오류] {Path}: 줄 {Line}, 열 {Col}", loadPath, jrEx.LineNumber, jrEx.LinePosition);
}
catch (Newtonsoft.Json.JsonSerializationException jsEx)
{
    result.Success = false;
    result.ErrorMessage = $"JSON 데이터 형식이 올바르지 않습니다.\n\n" +
        $"상세: {jsEx.Message}\n\n" +
        $"해결 방법: JSON 파일의 구조가 COCO 형식과 일치하는지 확인하세요.";
    Log.Warning("[JSON 역직렬화 오류] {Path}: {Message}", loadPath, jsEx.Message);
}
catch (IOException ioEx)
{
    result.Success = false;
    result.ErrorMessage = $"파일을 읽을 수 없습니다.\n\n" +
        $"파일: {Path.GetFileName(loadPath)}\n" +
        $"원인: {ioEx.Message}\n\n" +
        $"해결 방법: 파일이 다른 프로그램에서 사용 중인지 확인하세요.";
    Log.Warning("[파일 I/O 오류] {Path}: {Message}", loadPath, ioEx.Message);
}
catch (Exception ex)
{
    result.Success = false;
    result.ErrorMessage = $"라벨링 데이터 로드 중 예기치 않은 오류가 발생했습니다.\n\n" +
        $"상세: {ex.Message}\n\n" +
        $"해결 방법: 프로그램을 재시작하거나 파일을 다시 선택하세요.";
    Log.Error(ex, "[JSON 로드 오류] {Path}", loadPath);
}
```

주의: `loadPath` 변수가 catch 블록 스코프에서 접근 가능한지 확인. try 블록 내에서 정의되었으면 try 바깥으로 옮기거나 `videoFilePath`를 사용.

2. MAINT-02: JsonService.ExportToJsonExtended (line 559) catch 구체화:
```csharp
catch (IOException ioEx)
{
    throw new InvalidOperationException($"JSON 파일 저장 실패: {ioEx.Message}\n\n" +
        $"해결 방법: 저장 경로에 쓰기 권한이 있는지 확인하세요.", ioEx);
}
catch (UnauthorizedAccessException uaEx)
{
    throw new InvalidOperationException($"JSON 파일 접근 권한 없음: {uaEx.Message}\n\n" +
        $"해결 방법: 파일이 읽기 전용이 아닌지 확인하세요.", uaEx);
}
catch (Exception ex)
{
    throw new InvalidOperationException($"JSON 내보내기 오류: {ex.Message}", ex);
}
```

3. MAINT-02: JsonService backup catch (line 206):
```csharp
catch (IOException backupIoEx)
{
    System.Diagnostics.Debug.WriteLine($"[백업 생성 실패 - I/O] {backupIoEx.Message}");
    Log.Warning("[백업 생성 실패] I/O 오류: {Message}", backupIoEx.Message);
}
catch (UnauthorizedAccessException backupAuthEx)
{
    System.Diagnostics.Debug.WriteLine($"[백업 생성 실패 - 권한] {backupAuthEx.Message}");
    Log.Warning("[백업 생성 실패] 접근 권한 오류: {Message}", backupAuthEx.Message);
}
catch (Exception backupEx)
{
    System.Diagnostics.Debug.WriteLine($"[백업 생성 실패] {backupEx.Message}");
    Log.Warning("[백업 생성 실패] {Message}", backupEx.Message);
}
```

4. RELI-05 + MAINT-02: VideoService.LoadSrtFileAsync (line 475) 예외 처리 구체화:
```csharp
catch (FormatException fmtEx)
{
    Log.Warning("[자막 파싱 오류] SRT 파일 형식 오류: {Message}", fmtEx.Message);
    subtitleEntries.Clear();
}
catch (IOException ioEx)
{
    Log.Warning("[자막 로드 오류] 파일 읽기 실패: {Message}", ioEx.Message);
}
catch (Exception ex)
{
    Log.Warning("[자막 로드 오류] {Message}", ex.Message);
}
```

5. MAINT-02: VideoService.LoadFrame (line 211) 구체화:
```csharp
catch (OpenCvSharp.OpenCVException ocvEx)
{
    Log.Error(ocvEx, "[프레임 로드 오류 - OpenCV] {Message}", ocvEx.Message);
    return null;
}
catch (Exception ex)
{
    Log.Error(ex, "[프레임 로드 오류] {Message}", ex.Message);
    return null;
}
```
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "JsonReaderException" Services/JsonService.cs returns >= 1
    - grep -c "JsonSerializationException" Services/JsonService.cs returns >= 1
    - grep -c "FormatException" Services/VideoService.cs returns >= 1
    - grep -c "해결 방법" Services/JsonService.cs returns >= 3
    - grep -c "OpenCVException" Services/VideoService.cs returns >= 1
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>JsonService와 VideoService의 모든 catch(Exception)이 구체적 예외 타입으로 분리되고, 손상 파일 로드 시 사용자 안내 메시지가 표시된다</done>
</task>

<task type="auto">
  <name>Task 2: MainForm 예외 처리 구체화 + 사용자 메시지 개선</name>
  <files>Forms/MainForm.cs</files>
  <read_first>Forms/MainForm.cs (lines 270-303 LoadVideoWithSubtitle catch, lines 340-351 LoadFrame catch, lines 447-457 LoadLabelingData catch, lines 510-530 btnExportJson catch, lines 620-635 btnPlay catch, lines 688-699 btnExit catch)</read_first>
  <action>
MainForm.cs의 7개 catch(Exception) 블록을 구체적 예외 타입으로 분리하고, 사용자 메시지에 해결 방법을 추가한다 (per USAB-03, MAINT-02).

1. LoadVideoWithSubtitle (line 297):
기존 `catch (Exception ex)` 앞에 구체적 타입 추가:
```csharp
catch (OperationCanceledException)
{
    Log.Information("[영상 로드] 이전 로드 작업이 취소됨: {FilePath}", filePath);
}
catch (IOException ioEx)
{
    Log.Error(ioEx, "[영상 로드 오류 - I/O] {FilePath}", filePath);
    MessageBox.Show($"비디오 파일을 읽을 수 없습니다.\n\n" +
        $"파일: {Path.GetFileName(filePath)}\n" +
        $"원인: {ioEx.Message}\n\n" +
        $"해결 방법: 파일이 존재하고 다른 프로그램에서 사용 중이지 않은지 확인하세요.",
        "파일 오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
}
catch (OpenCvSharp.OpenCVException ocvEx)
{
    Log.Error(ocvEx, "[영상 로드 오류 - 코덱] {FilePath}", filePath);
    MessageBox.Show($"비디오 코덱을 지원하지 않습니다.\n\n" +
        $"파일: {Path.GetFileName(filePath)}\n" +
        $"원인: {ocvEx.Message}\n\n" +
        $"해결 방법: MP4(H.264) 형식의 비디오 파일을 사용하세요.",
        "코덱 오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
}
catch (Exception ex)
{
    Log.Error(ex, "[영상 로드 오류] {FilePath}", filePath);
    MessageBox.Show($"비디오 로드 중 오류가 발생했습니다.\n\n" +
        $"원인: {ex.Message}\n\n" +
        $"해결 방법: 프로그램을 재시작하거나 다른 비디오 파일을 시도하세요.",
        "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
}
```
주의: 기존 `catch (OperationCanceledException)` (line 293)이 이미 있으므로 중복 추가하지 않는다. 그 뒤의 `catch (Exception ex)`만 분리.

2. LoadFrame (line 347): 
```csharp
catch (OpenCvSharp.OpenCVException ocvEx)
{
    Log.Error(ocvEx, "[프레임 로드 오류 - OpenCV] Frame {Index}", frameIndex);
}
catch (Exception ex)
{
    Log.Error(ex, "[프레임 로드 오류] Frame {Index}", frameIndex);
}
```

3. LoadLabelingData (line 453):
```csharp
catch (IOException ioEx)
{
    Log.Error(ioEx, "[JSON 로드 오류 - I/O] {Message}", ioEx.Message);
}
catch (Exception ex)
{
    Log.Error(ex, "[JSON 로드 오류] {Message}", ex.Message);
}
```

4. btnExportJson inner catch (line 519):
```csharp
catch (InvalidOperationException invEx)
{
    loadingForm.Close();
    MessageBox.Show($"JSON 저장 실패:\n\n{invEx.Message}",
        "저장 오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
}
catch (IOException ioEx)
{
    loadingForm.Close();
    MessageBox.Show($"파일 저장 중 오류가 발생했습니다.\n\n" +
        $"원인: {ioEx.Message}\n\n" +
        $"해결 방법: 저장 경로의 디스크 공간과 쓰기 권한을 확인하세요.",
        "저장 오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
}
catch (Exception ex)
{
    loadingForm.Close();
    MessageBox.Show($"JSON 저장 중 예기치 않은 오류가 발생했습니다:\n{ex.Message}",
        "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
}
```

5. btnExportJson outer catch (line 525): 일반적 UI 오류이므로 그대로 유지 (이미 로깅됨).

6. btnPlay (line 628): 
```csharp
catch (InvalidOperationException invEx)
{
    Log.Error(invEx, "[재생 오류 - 상태] {Message}", invEx.Message);
    isPlaying = false; btnPlay.Text = "▶"; timerPlayback?.Stop();
}
catch (Exception ex)
{
    Log.Error(ex, "[재생 버튼 오류] {Message}", ex.Message);
    isPlaying = false; btnPlay.Text = "▶"; timerPlayback?.Stop();
}
```

7. btnExit (line 694):
```csharp
catch (InvalidOperationException invEx)
{
    Log.Error(invEx, "[Exit 설정 오류] {Message}", invEx.Message);
    MessageBox.Show($"Exit 마커 설정 실패:\n\n{invEx.Message}\n\n" +
        $"해결 방법: Entry 마커를 먼저 설정한 후 다시 시도하세요.",
        "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
}
catch (Exception ex)
{
    Log.Error(ex, "[Exit 버튼 오류] {Message}", ex.Message);
    MessageBox.Show($"Exit 설정 중 오류 발생:\n{ex.Message}",
        "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
}
```
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "해결 방법" Forms/MainForm.cs returns >= 3
    - grep -c "IOException" Forms/MainForm.cs returns >= 3
    - grep -c "OpenCVException" Forms/MainForm.cs returns >= 1
    - grep -c "InvalidOperationException" Forms/MainForm.cs returns >= 2
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>MainForm의 모든 catch(Exception)이 구체적 예외 타입으로 분리되고, 사용자에게 해결 방법을 포함한 안내 메시지가 표시된다</done>
</task>

</tasks>

<verification>
- `dotnet build -c Release --no-incremental` 성공
- JsonService, VideoService, MainForm 모든 파일에서 구체적 예외 타입이 generic catch 앞에 위치
- 손상된 JSON 로드 시 줄/열 위치 포함 메시지 표시
- 모든 사용자 오류 메시지에 "해결 방법" 포함
</verification>

<success_criteria>
- 손상된 JSON/SRT 파일 로드 시 크래시 없이 사용자 안내 메시지 표시
- 오류 메시지에 문제 설명 + 해결 방법이 포함됨
- generic catch(Exception)이 구체적 타입 처리 후 fallback으로만 존재
- 빌드 성공, 컴파일 오류 0건
</success_criteria>

<output>
After completion, create `.planning/phases/03-기능-정확성-보안/03-04-SUMMARY.md`
</output>
