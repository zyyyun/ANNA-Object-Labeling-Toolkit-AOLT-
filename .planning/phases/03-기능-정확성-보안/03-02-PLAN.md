---
phase: 03-기능-정확성-보안
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/SecurityHelper.cs
  - Helpers/PathValidator.cs
  - Services/JsonService.cs
  - Forms/MainForm.cs
autonomous: true
requirements: [SECU-01, SECU-04]
must_haves:
  truths:
    - "SecurityHelper.HashSecret이 PBKDF2-HMAC-SHA256으로 해싱하고 salt를 반환한다"
    - "SecurityHelper.VerifySecret이 CryptographicOperations.FixedTimeEquals로 타이밍 공격을 방지한다"
    - "PathValidator.IsPathSafe가 경로 트래버설(../)을 차단한다"
    - "파일 I/O 경로에 PathValidator 검증이 적용된다"
  artifacts:
    - path: "Helpers/SecurityHelper.cs"
      provides: "PBKDF2 hash/verify static methods"
      contains: "Rfc2898DeriveBytes.Pbkdf2"
    - path: "Helpers/PathValidator.cs"
      provides: "Path traversal prevention"
      contains: "IsPathSafe"
  key_links:
    - from: "Services/JsonService.cs"
      to: "Helpers/PathValidator.cs"
      via: "IsPathSafe call in ResolveJsonPath, SaveCurrentLabelingData, DeleteJsonFileForVideo"
      pattern: "PathValidator\\.IsPathSafe"
---

<objective>
KISA 가이드 준수를 위한 PBKDF2 해싱 모듈과 경로 트래버설 방지 모듈을 구현한다.

Purpose: GS인증 보안성 평가에서 암호화 강도와 입력 검증 기준 충족
Output: SecurityHelper.cs (PBKDF2), PathValidator.cs (경로 검증), JsonService/MainForm에 경로 검증 적용
</objective>

<execution_context>
@C:\Users\ANNA\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ANNA\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Services/JsonService.cs
@Helpers/CoordinateHelper.cs
</context>

<interfaces>
<!-- JsonService paths that need PathValidator protection -->
From Services/JsonService.cs:
```csharp
public string? ResolveJsonPath(string videoFilePath)        // line 115
public bool DeleteJsonFileForVideo(string videoFilePath, string? currentJsonFile = null)  // line 573
```

From Forms/MainForm.cs:
```csharp
private void SaveCurrentLabelingData()  // line 532 - builds path from videoDir + "labels"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SecurityHelper PBKDF2 + PathValidator 생성</name>
  <files>Helpers/SecurityHelper.cs, Helpers/PathValidator.cs</files>
  <read_first>Helpers/CoordinateHelper.cs (namespace/using 패턴 확인)</read_first>
  <action>
1. Helpers/SecurityHelper.cs 신규 생성 (per SECU-01):
```csharp
using System.Security.Cryptography;

namespace ASLTv1.Helpers
{
    /// <summary>
    /// KISA 가이드 준수 PBKDF2-HMAC-SHA256 해싱 유틸리티.
    /// 라이선스 검증 및 민감 데이터 해싱에 사용.
    /// </summary>
    public static class SecurityHelper
    {
        private const int SALT_SIZE = 16;
        private const int HASH_SIZE = 32;
        private const int ITERATIONS = 310_000;

        /// <summary>
        /// 입력 문자열을 PBKDF2-HMAC-SHA256으로 해싱합니다.
        /// </summary>
        /// <returns>Base64 인코딩된 (Hash, Salt) 튜플</returns>
        public static (string Hash, string Salt) HashSecret(string input)
        {
            if (string.IsNullOrEmpty(input))
                throw new ArgumentNullException(nameof(input));

            byte[] salt = RandomNumberGenerator.GetBytes(SALT_SIZE);
            byte[] hash = Rfc2898DeriveBytes.Pbkdf2(
                input, salt, ITERATIONS,
                HashAlgorithmName.SHA256, HASH_SIZE);
            return (Convert.ToBase64String(hash), Convert.ToBase64String(salt));
        }

        /// <summary>
        /// 입력 문자열이 저장된 해시와 일치하는지 검증합니다.
        /// 타이밍 공격 방지를 위해 FixedTimeEquals를 사용합니다.
        /// </summary>
        public static bool VerifySecret(string input, string storedHash, string storedSalt)
        {
            if (string.IsNullOrEmpty(input) || string.IsNullOrEmpty(storedHash) || string.IsNullOrEmpty(storedSalt))
                return false;

            byte[] salt = Convert.FromBase64String(storedSalt);
            byte[] candidateHash = Rfc2898DeriveBytes.Pbkdf2(
                input, salt, ITERATIONS,
                HashAlgorithmName.SHA256, HASH_SIZE);
            return CryptographicOperations.FixedTimeEquals(
                candidateHash, Convert.FromBase64String(storedHash));
        }
    }
}
```

2. Helpers/PathValidator.cs 신규 생성 (per SECU-04):
```csharp
namespace ASLTv1.Helpers
{
    /// <summary>
    /// 파일 경로 트래버설 방지 유틸리티.
    /// 모든 파일 I/O 전에 경로가 허용된 디렉토리 내에 있는지 검증.
    /// </summary>
    public static class PathValidator
    {
        /// <summary>
        /// 파일 경로가 허용된 기본 디렉토리 내에 있는지 검증합니다.
        /// </summary>
        /// <param name="filePath">검증할 파일 경로</param>
        /// <param name="allowedBaseDir">허용된 기본 디렉토리</param>
        /// <returns>경로가 안전하면 true</returns>
        public static bool IsPathSafe(string filePath, string allowedBaseDir)
        {
            if (string.IsNullOrWhiteSpace(filePath) || string.IsNullOrWhiteSpace(allowedBaseDir))
                return false;

            try
            {
                string fullPath = Path.GetFullPath(filePath);
                string fullBase = Path.GetFullPath(allowedBaseDir);

                // 기본 디렉토리가 경로 구분자로 끝나지 않으면 추가
                if (!fullBase.EndsWith(Path.DirectorySeparatorChar.ToString()))
                    fullBase += Path.DirectorySeparatorChar;

                return fullPath.StartsWith(fullBase, StringComparison.OrdinalIgnoreCase);
            }
            catch (ArgumentException)
            {
                return false;
            }
        }
    }
}
```
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f Helpers/SecurityHelper.cs (file exists)
    - test -f Helpers/PathValidator.cs (file exists)
    - grep -c "Rfc2898DeriveBytes.Pbkdf2" Helpers/SecurityHelper.cs returns >= 2
    - grep -c "FixedTimeEquals" Helpers/SecurityHelper.cs returns >= 1
    - grep -c "IsPathSafe" Helpers/PathValidator.cs returns >= 1
    - grep -c "Path.GetFullPath" Helpers/PathValidator.cs returns >= 2
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>SecurityHelper와 PathValidator가 컴파일되고, KISA 기준에 맞는 PBKDF2 해싱과 경로 검증 기능을 제공한다</done>
</task>

<task type="auto">
  <name>Task 2: PathValidator를 JsonService 파일 I/O에 적용</name>
  <files>Services/JsonService.cs, Forms/MainForm.cs</files>
  <read_first>Services/JsonService.cs (lines 115-132 ResolveJsonPath, lines 573-601 DeleteJsonFileForVideo), Forms/MainForm.cs (lines 532-553 SaveCurrentLabelingData)</read_first>
  <action>
JsonService.cs와 MainForm.cs의 파일 경로 구성 지점에 PathValidator.IsPathSafe 검증을 추가한다 (per SECU-04).

1. JsonService.cs 상단에 `using ASLTv1.Helpers;` 추가.

2. ResolveJsonPath (line 115-132):
`string normalPath = Path.Combine(saveDir, ...)` 이후, return 전에 검증 추가:
```csharp
if (File.Exists(normalPath))
{
    // 경로 트래버설 방지 검증
    if (!PathValidator.IsPathSafe(normalPath, videoDir))
    {
        Log.Warning("[보안] 경로 트래버설 감지: {Path}", normalPath);
        return null;
    }
    return normalPath;
}
```

3. DeleteJsonFileForVideo (line 573-601):
파일 삭제 전 경로 검증 추가. 삭제 대상 파일 경로에 대해:
```csharp
string? videoDir = Path.GetDirectoryName(videoFilePath);
if (string.IsNullOrEmpty(videoDir)) return false;

// currentJsonFile 경로 검증
if (!string.IsNullOrEmpty(currentJsonFile) && !PathValidator.IsPathSafe(currentJsonFile, videoDir))
{
    Log.Warning("[보안] 삭제 경로 트래버설 감지: {Path}", currentJsonFile);
    return false;
}
```

4. MainForm.cs SaveCurrentLabelingData (line 532-553):
savePath 생성 후, ExportToJsonExtended 호출 전에 검증 추가:
```csharp
string savePath = Path.Combine(saveDir, baseFileName + "_labels.json");

// 경로 트래버설 방지 검증
if (!PathValidator.IsPathSafe(savePath, videoDir))
{
    Log.Warning("[보안] 저장 경로 트래버설 감지: {Path}", savePath);
    MessageBox.Show("파일 경로가 올바르지 않습니다.", "보안 경고", MessageBoxButtons.OK, MessageBoxIcon.Warning);
    return;
}
```
MainForm.cs 상단에 `using ASLTv1.Helpers;` 추가 (이미 있으면 생략).
  </action>
  <verify>
    <automated>cd /c/Users/ANNA/AOLTv1.0 && dotnet build -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "PathValidator.IsPathSafe" Services/JsonService.cs returns >= 2
    - grep -c "PathValidator.IsPathSafe" Forms/MainForm.cs returns >= 1
    - grep -c "경로 트래버설" Services/JsonService.cs returns >= 1
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>모든 파일 I/O 경로에 트래버설 방지 검증이 적용되어 KISA 보안 기준을 충족한다</done>
</task>

</tasks>

<verification>
- `dotnet build -c Release --no-incremental` 성공
- SecurityHelper.cs에 PBKDF2 해싱 + 검증 메서드 존재
- PathValidator.cs에 IsPathSafe 메서드 존재
- JsonService.cs의 ResolveJsonPath, DeleteJsonFileForVideo에 PathValidator 적용
- MainForm.cs의 SaveCurrentLabelingData에 PathValidator 적용
</verification>

<success_criteria>
- PBKDF2-HMAC-SHA256 해싱이 310,000 iterations + 16byte salt로 구현
- FixedTimeEquals로 타이밍 공격 방지
- 모든 파일 경로 구성 지점에 트래버설 방지 적용
- 빌드 성공, 컴파일 오류 0건
</success_criteria>

<output>
After completion, create `.planning/phases/03-기능-정확성-보안/03-02-SUMMARY.md`
</output>
