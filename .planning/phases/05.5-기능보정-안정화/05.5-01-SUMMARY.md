---
phase: 05.5-기능보정-안정화
plan: 01
subsystem: Forms/MainForm
tags: [usability, reliability, waypoint, timeline, guard]
requires: [USAB-08, FUNC-08]
provides: [USAB-09, RELI-06]
affects: [Forms/MainForm.cs]
tech-stack:
  added: []
  patterns: [early-return-guard, dual-mode-button-branch]
key-files:
  created: []
  modified:
    - Forms/MainForm.cs
decisions:
  - "selectedWaypoint != null 시 Entry/Exit 버튼이 LoadFrame으로 이동하도록 분기 (D-01~D-05)"
  - "타임라인 가드는 !_videoService.IsVideoLoaded 단일 소스 사용 (D-06, D-07, D-10)"
  - "가드 실패 시 조용한 return (MessageBox/Log 없음) (D-08)"
  - "MouseUp의 isTimelineDragging = false 리셋은 조건 없이 항상 실행 (D-10, stuck-flag 방지)"
metrics:
  duration: "~2분"
  tasks: 2
  files_changed: 1
  completed: 2026-04-17
---

# Phase 05.5 Plan 01: 기능 보정 + 안정화 Summary

Forms/MainForm.cs에 Waypoint 이동 버튼 이중 기능 분기(USAB-09)와 영상 로드 경합 가드(RELI-06) 5곳 최소 수정 적용.

## Modified Locations

| Location                                     | Line (post-edit) | Change                                                                 |
| -------------------------------------------- | ---------------- | ---------------------------------------------------------------------- |
| `btnEntry_Click`                             | 815-824          | 람다를 블록으로 풀고 `if (selectedWaypoint != null) { LoadFrame(...); return; }` 분기 추가 |
| `btnExit_Click`                              | 834-838          | try 블록 이전에 `if (selectedWaypoint != null) { LoadFrame(...); return; }` 분기 삽입 |
| `panelTimeline_MouseDown`                    | 2106-2108        | 최상단에 `if (!_videoService.IsVideoLoaded) return;` 가드 추가                   |
| `panelTimeline_MouseMove`                    | 2119-2121        | 최상단에 `if (!_videoService.IsVideoLoaded) return;` 가드 추가                   |

## Preserved Locations (Regression Guard)

| Location                                     | Status                         |
| -------------------------------------------- | ------------------------------ |
| `SetEntryMarker()`                           | 시그니처 + 본문 불변 (USAB-08 연결 보존) |
| `SetExitMarkerAndCreateWaypoint()`           | 시그니처 + 본문 불변 (FUNC-08 연결 보존) |
| `panelTimeline_MouseUp`                      | 람다 그대로, 조건 없이 reset 유지        |
| `UpdateFrameFromTimeline(int mouseX)`        | 본문 불변                          |
| 키보드 단축키 Keys.E/Keys.X 핸들러                  | 손대지 않음 (CONTEXT.md D-01 scope 유지) |

## Grep Verification

```
selectedWaypoint != null                                                  : 4 (>=2 required, 2 new + 2 existing)
LoadFrame(selectedWaypoint.EntryFrame)                                    : 1
LoadFrame(selectedWaypoint.ExitFrame)                                     : 1
private void SetEntryMarker()                                             : 1 (preserved)
private async Task SetExitMarkerAndCreateWaypoint()                       : 1 (preserved)
!_videoService.IsVideoLoaded                                              : 7 (>=2 required, 2 new in timeline + 5 existing elsewhere)
private void panelTimeline_MouseUp(...) => isTimelineDragging = false;    : 1 (preserved)
private void UpdateFrameFromTimeline(int mouseX)                          : 1 (preserved)
```

All acceptance criteria satisfied.

## Build Result

`dotnet build ASLTv1.0.csproj -c Debug --nologo`:
- **C# compilation**: 0 errors, 0 new warnings (기존 38개 경고만 유지 — CS8632 nullable 주석, CS1998 async without await는 plan scope 외)
- **Output DLL**: `obj/Debug/net8.0-windows/win-x64/ASLTv1.dll` built fresh (2026-04-17 15:42)
- **Post-build copy**: MSB3021/MSB3027 2건 발생 — 실행 중인 AOLT 앱(PID 33536)이 `bin/.../ASLTv1.exe`를 잠그고 있어서 발생한 환경적 이슈 (코드 문제 아님). 앱 종료 후 재빌드 시 완전 통과 예정. CI/UAT 환경에서는 재발하지 않음.

## Deviations from Plan

None — 플랜이 명시한 5곳(btnEntry_Click, btnExit_Click, panelTimeline_MouseDown, panelTimeline_MouseMove, + MouseUp 보존 확인)과 CONTEXT.md D-01~D-10 결정사항을 문자 그대로 따랐음. 추가 픽스 없음, 새 필드/이벤트/알림 도입 없음.

## Commit

- `09a549b` — `fix(05.5-01): Waypoint 이동 버튼 분기 + 타임라인 로드 가드 (USAB-09, RELI-06)`

## Manual UAT (Deferred)

Phase 6 문서화 이전 별도 세션에서 실행 예정. CONTEXT.md §specifics 시나리오:
- USAB-09 A/B/C: Waypoint 선택/미선택 Entry/Exit 버튼 동작, 키보드 단축키 기존 동작 보존
- RELI-06 A/B/C: 영상 로드 직전/중/후 타임라인 클릭·드래그 동작, MouseUp stuck-flag 방지

## Known Stubs

None.

## Self-Check: PASSED

- `Forms/MainForm.cs` modified and committed in `09a549b` — FOUND
- SUMMARY.md written to `.planning/phases/05.5-기능보정-안정화/05.5-01-SUMMARY.md` — FOUND
- C# compile: 0 errors verified
- All grep acceptance criteria: met
