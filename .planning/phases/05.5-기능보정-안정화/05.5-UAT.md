---
status: complete
phase: 05.5-기능보정-안정화
source:
  - .planning/phases/05.5-기능보정-안정화/05.5-01-SUMMARY.md
started: 2026-04-17T00:00:00Z
updated: 2026-04-17T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. USAB-09 A — Waypoint 선택 후 Entry/Exit 버튼으로 이동
expected: Waypoint 선택 상태에서 Entry/Exit 버튼이 해당 Waypoint의 프레임으로 이동만 수행. 새 Waypoint 생성 없음.
result: pass

### 2. USAB-09 B — Waypoint 미선택 시 기존 동작 유지 (회귀 방지)
expected: Waypoint를 선택하지 않은 상태(또는 Escape로 선택 해제 후)에서 Entry/Exit 버튼 클릭 → 기존대로 현재 프레임을 Entry로 지정 또는 Exit로 Waypoint 생성. 기존 동작과 100% 동일.
result: pass

### 3. USAB-09 C — Waypoint 선택 + bbox 선택 병행 시
expected: Waypoint 선택 상태에서 프레임 내 bbox를 클릭해 selectedBox 설정. Entry/Exit 버튼은 여전히 **selectedWaypoint 기준으로 이동 모드**로 동작 (새 Entry/Exit 지정 아님).
result: pass

### 4. RELI-06 A — 영상 로드 중 타임라인 클릭 가드
expected: 영상 파일 선택 → 로드 진행 중(프레임 렌더링 전) 즉시 타임라인 클릭/드래그 → 앱 반응 없음 (클릭 무시됨). 렉, 크래시, 에러 메시지 없이 조용히 무시.
result: issue
reported: "렉은 여전히 발생. 다만 예전에는 재생 속도까지 느려졌다면, 지금은 초당 1프레임씩 로드되는 여전히 버그 현상은 존재하지만 속도 자체가 느려지진 않는 것 같음"
severity: major
partial_fix: "재생 속도 저하 부분은 해결됐으나 초기 프레임 로드 지연(1 fps) 현상은 잔존"

### 5. RELI-06 B — 로드 완료 후 타임라인 정상 동작 (회귀 방지)
expected: 영상 로드 완전히 완료 후 → 타임라인 클릭 → 해당 위치 프레임으로 이동. 드래그도 정상. 기존 동작과 100% 동일.
result: pass

### 6. RELI-06 C — 수동 추적 + 영상 전환 경합 방지
expected: Person 또는 Vehicle bbox를 드래그한 상태(마우스 버튼 누른 채)에서 [파일 열기]로 새 영상 선택 → 기존 영상 종료 + 새 영상 로드 중 타임라인 클릭 시 무시됨. 크래시/렉 없음.
result: pass

## Summary

total: 6
passed: 5
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "영상 로드 중 타임라인 상호작용 시 렉/크래시가 발생하지 않는다"
  status: partial_fix
  reason: "User reported: 렉은 여전히 발생. 다만 예전에는 재생 속도까지 느려졌다면, 지금은 초당 1프레임씩 로드되는 여전히 버그 현상은 존재하지만 속도 자체가 느려지진 않는 것 같음"
  severity: major
  test: 4
  improvement: "재생 속도 저하 제거됨 — panelTimeline guard가 부분적으로 효과 있음"
  remaining: "초기 프레임 로드가 1fps 수준으로 느려짐 — 타임라인 가드만으로는 완전 해결 안 됨"
  hypothesis:
    - "WinForms 이벤트 큐에 대기 중인 MouseDown이 IsVideoLoaded=true 된 후 실행되어 한 번의 LoadFrame 트리거"
    - "또는 panelTimeline 외 다른 경로(키보드, 재생 타이머)에서 프레임 로드가 발생해 경합"
    - "또는 실제 비디오 코덱 디코딩이 초기에는 느려 1fps처럼 보일 수 있음 (seek 후 키프레임 재검색)"
  artifacts: []
  missing:
    - "추가 가드 경로 분석 (keyboard shortcuts, playback timer, seek operations)"
    - "LoadVideoAsync 완료 시점과 IsVideoLoaded 플래그 설정 순서 확인"
    - "OpenCvSharp VideoCapture seek 성능 확인 (1fps 원인 후보)"
