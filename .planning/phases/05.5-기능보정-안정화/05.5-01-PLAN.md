---
phase: 05.5-기능보정-안정화
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Forms/MainForm.cs
autonomous: true
requirements:
  - USAB-09
  - RELI-06

must_haves:
  truths:
    - "Waypoint 선택 상태에서 Entry 버튼 클릭 시 selectedWaypoint.EntryFrame으로 LoadFrame 호출된다"
    - "Waypoint 선택 상태에서 Exit 버튼 클릭 시 selectedWaypoint.ExitFrame으로 LoadFrame 호출된다"
    - "Waypoint 미선택 상태에서 Entry/Exit 버튼은 기존 SetEntryMarker / SetExitMarkerAndCreateWaypoint 동작을 수행한다"
    - "영상이 로드되지 않은 상태에서 panelTimeline_MouseDown은 조용히 return하여 UpdateFrameFromTimeline을 호출하지 않는다"
    - "영상이 로드되지 않은 상태에서 panelTimeline_MouseMove는 조용히 return하여 UpdateFrameFromTimeline을 호출하지 않는다"
    - "panelTimeline_MouseUp은 조건과 무관하게 isTimelineDragging = false로 리셋한다"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "btnEntry_Click / btnExit_Click 이중 기능 분기 + panelTimeline 가드"
      contains: "selectedWaypoint != null"
  key_links:
    - from: "btnEntry_Click"
      to: "LoadFrame(selectedWaypoint.EntryFrame)"
      via: "selectedWaypoint 분기 return"
      pattern: "selectedWaypoint\\.EntryFrame"
    - from: "btnExit_Click"
      to: "LoadFrame(selectedWaypoint.ExitFrame)"
      via: "selectedWaypoint 분기 return"
      pattern: "selectedWaypoint\\.ExitFrame"
    - from: "panelTimeline_MouseDown / MouseMove"
      to: "early return"
      via: "_videoService.IsVideoLoaded guard"
      pattern: "!_videoService\\.IsVideoLoaded"
---

<objective>
Phase 5.5 (기능 보정 + 안정화) 잠긴 바이너리 기능 완성. Waypoint 탐색 편의(USAB-09)와 영상 로드 경합 가드(RELI-06)를 동일 파일(Forms/MainForm.cs)의 서로 다른 영역에서 최소 수정으로 구현한다.

Purpose: GS인증 1등급 기준(ISO/IEC 25023) 사용성·신뢰성 특성 달성. 사용자 현장 테스트에서 보고된 2건의 잠재 이슈를 해결해 Phase 6 문서화 이전에 바이너리를 잠근다.
Output: Forms/MainForm.cs 수정 (btnEntry_Click, btnExit_Click, panelTimeline_MouseDown/Move/Up 5곳)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.5-기능보정-안정화/05.5-CONTEXT.md

@Forms/MainForm.cs
@Services/VideoService.cs
@Models/WaypointMarker.cs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-04-17. -->

From Forms/MainForm.cs (field at line 49):
```csharp
private WaypointMarker selectedWaypoint = null;
```

From Models/WaypointMarker.cs (required fields used in this plan):
```csharp
public int EntryFrame { get; set; }
public int ExitFrame { get; set; }
```

From Services/VideoService.cs:
```csharp
public bool IsVideoLoaded { get; }   // true AFTER LoadVideoAsync completes
public int TotalFrames { get; }
```

From Forms/MainForm.cs (existing method, DO NOT modify — reuse only):
```csharp
private void LoadFrame(int frameIndex)   // ~line 325; already guards IsVideoLoaded internally
private void SetEntryMarker()            // line 817; DO NOT modify signature/body
private async Task SetExitMarkerAndCreateWaypoint()  // line 845; DO NOT modify signature/body
```

Current code to modify (exact strings, verified 2026-04-17):

Forms/MainForm.cs line 815:
```csharp
private void btnEntry_Click(object sender, EventArgs e) => SetEntryMarker();
```

Forms/MainForm.cs lines 825-843:
```csharp
private async void btnExit_Click(object sender, EventArgs e)
{
    try
    {
        await SetExitMarkerAndCreateWaypoint();
    }
    catch (InvalidOperationException invEx)
    {
        Log.Error(invEx, "[Exit 설정 오류] {Message}", invEx.Message);
        MessageBox.Show($"Exit 마커 설정 실패:\n\n{invEx.Message}\n\n" +
            $"해결 방법: Entry 마커를 먼저 설정한 후 다시 시도하세요.",
            "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
    }
    catch (Exception ex)
    {
        Log.Error(ex, "[Exit 버튼 오류] {Message}", ex.Message);
        MessageBox.Show($"Exit 설정 중 오류 발생:\n{ex.Message}", "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
    }
}
```

Forms/MainForm.cs lines 2097-2116:
```csharp
private void panelTimeline_MouseDown(object sender, MouseEventArgs e)
{
    if (_videoService.TotalFrames == 0) return;

    // 세그먼트 클릭 감지 먼저
    if (TryNavigateToMarker(e.X, e.Y))
        return;

    // 세그먼트가 아니면 드래그 시작
    isTimelineDragging = true;
    UpdateFrameFromTimeline(e.X);
}

private void panelTimeline_MouseMove(object sender, MouseEventArgs e)
{
    if (isTimelineDragging && _videoService.TotalFrames > 0)
        UpdateFrameFromTimeline(e.X);
}

private void panelTimeline_MouseUp(object sender, MouseEventArgs e) => isTimelineDragging = false;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: USAB-09 — Entry/Exit 버튼 이중 기능 분기 추가</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - Forms/MainForm.cs lines 815-843 (btnEntry_Click, btnExit_Click 현재 구현)
    - Forms/MainForm.cs line 49 (selectedWaypoint 필드 선언)
    - Forms/MainForm.cs line 325 근방 (LoadFrame 시그니처 및 내부 IsVideoLoaded 가드)
    - Forms/MainForm.cs line 817, 845 (SetEntryMarker, SetExitMarkerAndCreateWaypoint 본문 — 수정 금지 확인)
    - Models/WaypointMarker.cs (EntryFrame, ExitFrame 프로퍼티)
    - .planning/phases/05.5-기능보정-안정화/05.5-CONTEXT.md §decisions D-01~D-05
  </read_first>
  <behavior>
    - Test 1 (USAB-09 A): selectedWaypoint != null 상태에서 btnEntry 클릭 → LoadFrame(selectedWaypoint.EntryFrame) 호출되고 SetEntryMarker는 호출되지 않음
    - Test 2 (USAB-09 A): selectedWaypoint != null 상태에서 btnExit 클릭 → LoadFrame(selectedWaypoint.ExitFrame) 호출되고 SetExitMarkerAndCreateWaypoint는 호출되지 않음
    - Test 3 (USAB-09 B): selectedWaypoint == null 상태에서 btnEntry 클릭 → 기존대로 SetEntryMarker() 호출 (entryFrameIndex 갱신 + btnEntry.Text 갱신)
    - Test 4 (USAB-09 B): selectedWaypoint == null 상태에서 btnExit 클릭 → 기존대로 await SetExitMarkerAndCreateWaypoint() 호출 (try/catch 경로 포함 보존)
    - Regression: SetEntryMarker / SetExitMarkerAndCreateWaypoint 본문은 문자 1자도 수정되지 않음
  </behavior>
  <action>
1. `Forms/MainForm.cs` line 815의 람다 표현식 형태를 블록 형태로 풀어서 분기 추가 (D-01, D-02, D-03 per CONTEXT.md):

   AS-IS (line 815):
   ```csharp
   private void btnEntry_Click(object sender, EventArgs e) => SetEntryMarker();
   ```

   TO-BE:
   ```csharp
   private void btnEntry_Click(object sender, EventArgs e)
   {
       // USAB-09: Waypoint 선택 상태이면 해당 EntryFrame으로 이동, 아니면 기존 지정 동작
       if (selectedWaypoint != null)
       {
           LoadFrame(selectedWaypoint.EntryFrame);
           return;
       }
       SetEntryMarker();
   }
   ```

2. `Forms/MainForm.cs` lines 825-843의 btnExit_Click은 try 블록 진입 전에 분기 추가. 기존 try/catch 두 블록과 Log.Error / MessageBox 경로는 그대로 보존:

   AS-IS (line 825):
   ```csharp
   private async void btnExit_Click(object sender, EventArgs e)
   {
       try
       {
           await SetExitMarkerAndCreateWaypoint();
       }
       catch (InvalidOperationException invEx)
       {
           // ... (기존 본문 그대로)
       }
       catch (Exception ex)
       {
           // ... (기존 본문 그대로)
       }
   }
   ```

   TO-BE:
   ```csharp
   private async void btnExit_Click(object sender, EventArgs e)
   {
       // USAB-09: Waypoint 선택 상태이면 해당 ExitFrame으로 이동, 아니면 기존 지정 동작
       if (selectedWaypoint != null)
       {
           LoadFrame(selectedWaypoint.ExitFrame);
           return;
       }
       try
       {
           await SetExitMarkerAndCreateWaypoint();
       }
       catch (InvalidOperationException invEx)
       {
           Log.Error(invEx, "[Exit 설정 오류] {Message}", invEx.Message);
           MessageBox.Show($"Exit 마커 설정 실패:\n\n{invEx.Message}\n\n" +
               $"해결 방법: Entry 마커를 먼저 설정한 후 다시 시도하세요.",
               "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
       }
       catch (Exception ex)
       {
           Log.Error(ex, "[Exit 버튼 오류] {Message}", ex.Message);
           MessageBox.Show($"Exit 설정 중 오류 발생:\n{ex.Message}", "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
       }
   }
   ```

3. 제약:
   - SetEntryMarker (line 817) 및 SetExitMarkerAndCreateWaypoint (line 845) 본문은 수정 금지 — USAB-08(Phase 4) LoadFrame rebind 로직 및 FUNC-08(Phase 3) ChangeBoxIdWithinWaypoint 회귀 방지.
   - Line 2400-2401의 키보드 단축키(Keys.E / Keys.X) 핸들러는 건드리지 않음 — 이동 기능은 버튼 경로에만 적용 (CONTEXT.md D-01 scope).
   - 새 필드 선언 금지, 새 이벤트 구독 금지.
  </action>
  <verify>
    <automated>
      cd /c/Users/ANNA/AOLTv1.0 && dotnet build ASLTv1.0.csproj -c Debug --nologo -v:minimal 2>&1 | tee /tmp/build.log && \
      grep -c "selectedWaypoint != null" Forms/MainForm.cs | awk '{ if ($1 >= 2) print "OK waypoint branches:", $1; else { print "FAIL branches:", $1; exit 1 } }' && \
      grep -c "LoadFrame(selectedWaypoint.EntryFrame)" Forms/MainForm.cs | awk '{ if ($1 >= 1) print "OK EntryFrame link"; else { print "FAIL EntryFrame link missing"; exit 1 } }' && \
      grep -c "LoadFrame(selectedWaypoint.ExitFrame)" Forms/MainForm.cs | awk '{ if ($1 >= 1) print "OK ExitFrame link"; else { print "FAIL ExitFrame link missing"; exit 1 } }' && \
      grep -q "private void SetEntryMarker()" Forms/MainForm.cs && echo "OK SetEntryMarker preserved" && \
      grep -q "private async Task SetExitMarkerAndCreateWaypoint()" Forms/MainForm.cs && echo "OK SetExitMarkerAndCreateWaypoint preserved"
    </automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build -c Debug` exits 0 (no compile errors)
    - `grep -c "selectedWaypoint != null" Forms/MainForm.cs` returns >= 2 (one in btnEntry_Click, one in btnExit_Click)
    - `grep -c "LoadFrame(selectedWaypoint.EntryFrame)" Forms/MainForm.cs` returns >= 1
    - `grep -c "LoadFrame(selectedWaypoint.ExitFrame)" Forms/MainForm.cs` returns >= 1
    - `grep -c "private void SetEntryMarker()" Forms/MainForm.cs` returns 1 (preserved)
    - `grep -c "private async Task SetExitMarkerAndCreateWaypoint()" Forms/MainForm.cs` returns 1 (preserved)
    - `grep -c "=> SetEntryMarker();" Forms/MainForm.cs` returns 0 (lambda unwrapped) — line 2400 uses `{ SetEntryMarker(); e.Handled = true; }` not `=>`, so should still be 0
  </acceptance_criteria>
  <done>
    - btnEntry_Click은 selectedWaypoint != null 분기에서 LoadFrame(selectedWaypoint.EntryFrame) 호출 후 return
    - btnExit_Click은 selectedWaypoint != null 분기에서 LoadFrame(selectedWaypoint.ExitFrame) 호출 후 return (try 블록 이전)
    - selectedWaypoint == null 경로에서는 기존 SetEntryMarker / SetExitMarkerAndCreateWaypoint가 호출됨 (try/catch 체인 보존)
    - 빌드 성공
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RELI-06 — 영상 로드 중 타임라인 상호작용 가드</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - Forms/MainForm.cs lines 2097-2126 (panelTimeline_MouseDown / MouseMove / MouseUp / UpdateFrameFromTimeline 현재 구현)
    - Services/VideoService.cs (IsVideoLoaded 프로퍼티 정의)
    - Forms/MainForm.cs line 325 근방 (LoadFrame 내부 IsVideoLoaded 가드 — 중복 가드 확인)
    - .planning/phases/05.5-기능보정-안정화/05.5-CONTEXT.md §decisions D-06~D-10
  </read_first>
  <behavior>
    - Test 1 (RELI-06 A): _videoService.IsVideoLoaded == false 상태에서 panelTimeline_MouseDown 호출 → TryNavigateToMarker, isTimelineDragging 세팅, UpdateFrameFromTimeline 모두 호출되지 않음 (조용히 return)
    - Test 2 (RELI-06 A): _videoService.IsVideoLoaded == false 상태에서 panelTimeline_MouseMove 호출 → UpdateFrameFromTimeline 호출되지 않음
    - Test 3 (RELI-06 B): _videoService.IsVideoLoaded == true 상태에서는 기존 동작 완전 보존 (TryNavigateToMarker 진입, 드래그 시작, UpdateFrameFromTimeline 호출 경로 그대로)
    - Test 4: panelTimeline_MouseUp은 IsVideoLoaded 상태와 무관하게 isTimelineDragging = false로 리셋 (stuck flag 방지, D-10)
    - Regression: isTimelineDragging 상태 머신 변경 없음, TryNavigateToMarker / UpdateFrameFromTimeline 본문 변경 없음, MessageBox/Log 경로 추가 없음 (D-08 조용한 가드)
  </behavior>
  <action>
1. `Forms/MainForm.cs` line 2097의 panelTimeline_MouseDown 최상단에 IsVideoLoaded 가드 추가 (D-06, D-07 per CONTEXT.md):

   AS-IS (line 2097):
   ```csharp
   private void panelTimeline_MouseDown(object sender, MouseEventArgs e)
   {
       if (_videoService.TotalFrames == 0) return;

       // 세그먼트 클릭 감지 먼저
       if (TryNavigateToMarker(e.X, e.Y))
           return;

       // 세그먼트가 아니면 드래그 시작
       isTimelineDragging = true;
       UpdateFrameFromTimeline(e.X);
   }
   ```

   TO-BE:
   ```csharp
   private void panelTimeline_MouseDown(object sender, MouseEventArgs e)
   {
       // RELI-06: 영상 로드 완료 전 타임라인 입력 무시 (조용히 return, D-08)
       if (!_videoService.IsVideoLoaded) return;
       if (_videoService.TotalFrames == 0) return;

       // 세그먼트 클릭 감지 먼저
       if (TryNavigateToMarker(e.X, e.Y))
           return;

       // 세그먼트가 아니면 드래그 시작
       isTimelineDragging = true;
       UpdateFrameFromTimeline(e.X);
   }
   ```

2. `Forms/MainForm.cs` line 2110의 panelTimeline_MouseMove 최상단에 IsVideoLoaded 가드 추가:

   AS-IS (line 2110):
   ```csharp
   private void panelTimeline_MouseMove(object sender, MouseEventArgs e)
   {
       if (isTimelineDragging && _videoService.TotalFrames > 0)
           UpdateFrameFromTimeline(e.X);
   }
   ```

   TO-BE:
   ```csharp
   private void panelTimeline_MouseMove(object sender, MouseEventArgs e)
   {
       // RELI-06: 영상 로드 완료 전 드래그 이동 무시
       if (!_videoService.IsVideoLoaded) return;
       if (isTimelineDragging && _videoService.TotalFrames > 0)
           UpdateFrameFromTimeline(e.X);
   }
   ```

3. `Forms/MainForm.cs` line 2116의 panelTimeline_MouseUp은 **변경하지 않음** (D-10 per CONTEXT.md — isTimelineDragging = false 리셋은 stuck flag 방지를 위해 항상 실행):

   PRESERVE AS-IS:
   ```csharp
   private void panelTimeline_MouseUp(object sender, MouseEventArgs e) => isTimelineDragging = false;
   ```

4. 제약:
   - UpdateFrameFromTimeline (line 2118) 본문은 수정 금지 — 이 메서드는 LoadFrame 호출 전 IsVideoLoaded 체크에 간접 의존.
   - 새 필드/플래그 도입 금지 (_videoLoadCts 등 기존 인프라 재사용하지 않음, D-10 per CONTEXT.md는 IsVideoLoaded 단일 소스만 사용).
   - 가드 시 MessageBox / Log 호출 금지 (D-08 조용한 가드).
   - USAB-08 (Phase 4) LoadFrame rebind 로직, FUNC-08 (Phase 3) ChangeBoxIdWithinWaypoint 경로는 건드리지 않음.
  </action>
  <verify>
    <automated>
      cd /c/Users/ANNA/AOLTv1.0 && dotnet build ASLTv1.0.csproj -c Debug --nologo -v:minimal 2>&1 | tee -a /tmp/build.log && \
      grep -c "!_videoService.IsVideoLoaded" Forms/MainForm.cs | awk '{ if ($1 >= 2) print "OK timeline guards:", $1; else { print "FAIL guards:", $1; exit 1 } }' && \
      grep -q "private void panelTimeline_MouseUp(object sender, MouseEventArgs e) => isTimelineDragging = false;" Forms/MainForm.cs && echo "OK MouseUp preserved" && \
      grep -q "private void UpdateFrameFromTimeline(int mouseX)" Forms/MainForm.cs && echo "OK UpdateFrameFromTimeline preserved"
    </automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build -c Debug` exits 0
    - `grep -c "!_videoService.IsVideoLoaded" Forms/MainForm.cs` returns >= 2 (one in MouseDown, one in MouseMove)
    - `grep -c "private void panelTimeline_MouseUp(object sender, MouseEventArgs e) => isTimelineDragging = false;" Forms/MainForm.cs` returns 1 (MouseUp unchanged)
    - `grep -c "private void UpdateFrameFromTimeline(int mouseX)" Forms/MainForm.cs` returns 1 (preserved)
    - No new MessageBox calls added in panelTimeline_MouseDown/Move/Up regions (lines 2097-2116 area)
    - No new field declarations added for this guard
  </acceptance_criteria>
  <done>
    - panelTimeline_MouseDown과 panelTimeline_MouseMove 최상단에 `if (!_videoService.IsVideoLoaded) return;` 가드 추가됨
    - panelTimeline_MouseUp 시그니처 및 본문 불변
    - UpdateFrameFromTimeline 본문 불변
    - 가드 실패 시 사용자 알림(Messagebox/Log) 없음
    - 빌드 성공
  </done>
</task>

</tasks>

<verification>
플랜 전체 완료 검증:

1. 빌드: `cd /c/Users/ANNA/AOLTv1.0 && dotnet build ASLTv1.0.csproj -c Debug --nologo` exits 0
2. USAB-09 grep 검증:
   - `grep -n "selectedWaypoint != null" Forms/MainForm.cs` → 2+ hits, 최소 하나는 btnEntry_Click (~line 815-825), 하나는 btnExit_Click (~line 825-850)
   - `grep -n "LoadFrame(selectedWaypoint.EntryFrame)" Forms/MainForm.cs` → 1+ hit
   - `grep -n "LoadFrame(selectedWaypoint.ExitFrame)" Forms/MainForm.cs` → 1+ hit
3. RELI-06 grep 검증:
   - `grep -n "!_videoService.IsVideoLoaded" Forms/MainForm.cs` → 2+ hits in panelTimeline region (~line 2097-2116)
4. Regression 검증 (회귀 방지):
   - `grep -n "private void SetEntryMarker()" Forms/MainForm.cs` → 1 hit (시그니처 보존)
   - `grep -n "private async Task SetExitMarkerAndCreateWaypoint()" Forms/MainForm.cs` → 1 hit (시그니처 보존)
   - `grep -n "private void UpdateFrameFromTimeline(int mouseX)" Forms/MainForm.cs` → 1 hit (보존)
   - MouseUp 람다 표현식 보존 검증
5. 수동 UAT (Phase 6 전 최종 스모크 — 별도 세션):
   - USAB-09 A/B/C: CONTEXT.md §specifics 시나리오 1~3
   - RELI-06 A/B/C: CONTEXT.md §specifics 시나리오 4~6
</verification>

<success_criteria>
- USAB-09: Waypoint 선택 상태 → Entry/Exit 버튼이 해당 Waypoint의 EntryFrame/ExitFrame으로 LoadFrame 호출 (grep으로 연결 검증 가능)
- USAB-09: Waypoint 미선택 상태 → Entry/Exit 버튼이 기존 SetEntryMarker / SetExitMarkerAndCreateWaypoint 호출 (시그니처 보존 검증 가능)
- RELI-06: 영상 미로드 상태에서 panelTimeline MouseDown/Move가 UpdateFrameFromTimeline을 호출하지 않음 (!IsVideoLoaded early return)
- RELI-06: MouseUp의 isTimelineDragging = false 리셋은 조건 없이 실행 (stuck flag 방지)
- `dotnet build -c Debug` 성공
- 기존 USAB-08 (Phase 4) LoadFrame rebind, FUNC-08 (Phase 3) ChangeBoxIdWithinWaypoint 경로 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-기능보정-안정화/05.5-01-SUMMARY.md` summarizing:
- Modified locations with line numbers (btnEntry_Click, btnExit_Click, panelTimeline_MouseDown, panelTimeline_MouseMove)
- Preserved locations (SetEntryMarker, SetExitMarkerAndCreateWaypoint, panelTimeline_MouseUp, UpdateFrameFromTimeline)
- Grep verification output
- Build result
- Any manual UAT notes deferred to follow-up
</output>
