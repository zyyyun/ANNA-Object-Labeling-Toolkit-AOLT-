---
phase: 05.5-기능보정-안정화
plan: 02
type: execute
wave: 1
depends_on: [05.5-01]
files_modified:
  - Forms/MainForm.cs
  - Services/VideoService.cs
autonomous: true
gap_closure: true
requirements: [RELI-06]
user_setup: []

must_haves:
  truths:
    - "영상 로드 직후 초기 프레임이 코덱 cold-start 중에도 1fps 수준의 지연 없이 렌더링된다"
    - "로드 진행 중 타이머/키보드/Rewind-Forward/Waypoint 리스트 클릭이 LoadFrame 을 트리거하지 않는다"
    - "로드 완료 후 모든 경로(타임라인, 키, 타이머, Rewind/Forward, Waypoint 클릭)가 회귀 없이 정상 동작한다"
    - "초기 LoadFrame(0) 호출이 중복 실행되지 않는다"
    - "자동 재생은 첫 프레임이 실제 화면에 페인트된 이후에 시작된다"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "_isVideoReady flag + unified guard across LoadFrame entry points + deferred auto-play"
      contains: "_isVideoReady"
    - path: "Services/VideoService.cs"
      provides: "Removal of duplicate LoadFrame(0) in LoadVideoAsync"
      contains: "currentFrameIndex = 0"
  key_links:
    - from: "Forms/MainForm.cs timerPlayback_Tick"
      to: "_isVideoReady guard"
      via: "early return when !_isVideoReady"
      pattern: "_isVideoReady"
    - from: "Forms/MainForm.cs ProcessCmdKey"
      to: "_isVideoReady guard"
      via: "early return from Left/Right/Shift+Left/Shift+Right branches"
      pattern: "_isVideoReady"
    - from: "Forms/MainForm.cs btnRewind_Click / btnForward_Click"
      to: "_isVideoReady guard"
      via: "early return when !_isVideoReady"
      pattern: "_isVideoReady"
    - from: "Forms/MainForm.cs listViewPersonWaypoints_Click / listViewVehicleWaypoints_Click / listViewEventWaypoints_Click / TryNavigateToMarker"
      to: "_isVideoReady guard"
      via: "early return before LoadFrame(waypoint.EntryFrame)"
      pattern: "_isVideoReady"
    - from: "Forms/MainForm.cs LoadVideoWithSubtitle"
      to: "first-paint based auto-play trigger"
      via: "_pendingAutoPlay flag set after LoadLabelingData, consumed by pictureBoxVideo.Paint once"
      pattern: "_pendingAutoPlay"
---

<objective>
RELI-06 Phase 5.5 초기 수정(타임라인 가드)이 해결하지 못한 "영상 로드 중 ~1fps 지연" 현상을 제거한다. 타임라인 외 경로(재생 타이머, 키보드 좌우, Rewind/Forward, Waypoint 리스트 클릭)에서도 cold-decoder 기간 동안 LoadFrame 호출을 차단하고, 중복 LoadFrame(0) 및 premature 자동재생을 제거한다.

Purpose: cold OpenCV decoder 구간(~2-3초)에서 발생하는 blocking `videoCapture.Set(PosFrames, N)` 키프레임 재검색의 병렬/순차 호출 폭주를 근절한다.
Output:
- `_isVideoReady` 통합 가드 플래그
- 4개 경로에 guard 추가
- LoadVideoAsync 의 중복 LoadFrame(0) 제거
- 자동재생을 first-paint 이후로 연기
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-기능보정-안정화/05.5-CONTEXT.md
@.planning/phases/05.5-기능보정-안정화/05.5-UAT.md
@.planning/phases/05.5-기능보정-안정화/RELI-06-diagnosis.md
@.planning/phases/05.5-기능보정-안정화/05.5-01-SUMMARY.md
@Forms/MainForm.cs
@Services/VideoService.cs

<interfaces>
<!-- Key existing symbols executor must reference exactly. Do not re-explore. -->

From Forms/MainForm.cs (fields, ~line 45-75):
```csharp
private WaypointMarker selectedWaypoint;    // line 49
private bool isDragging;                     // line 63
private bool isResizing;                     // line 64
// NEW field to add here:
// private bool _isVideoReady;               // set true only after first frame paints AND LoadLabelingData returns
// private bool _pendingAutoPlay;            // set before first-paint handshake, consumed once
```

From Forms/MainForm.cs LoadVideoWithSubtitle (~line 289-322):
```csharp
await _videoService.LoadVideoAsync(filePath, token);  // line 289
// ... first frame paint (line 302-307) ...
await LoadLabelingData(filePath);                     // line 316
if (!isPlaying) { btnPlay_Click(null, EventArgs.Empty); }  // line 319-322  <-- MOVE
```

From Forms/MainForm.cs LoadFrame (~line 356-433):
```csharp
private void LoadFrame(int frameIndex)
{
    try
    {
        if (!_videoService.IsVideoLoaded) return;         // line 360 (existing)
        if (frameIndex < 0 || frameIndex >= _videoService.TotalFrames) return;
        // NEW: add _isVideoReady guard here — BUT we also add guards at CALLERS so this is belt-and-suspenders
        ...
    }
}
```

From Forms/MainForm.cs call sites that must be guarded:
- timerPlayback_Tick      line 774  → LoadFrame(nextFrame)  line 784
- btnRewind_Click         line 797  → LoadFrame(newFrame)   line 801
- btnForward_Click        line 804  → LoadFrame(newFrame)   line 808
- listViewPersonWaypoints_Click  line ~1110 → LoadFrame(waypoint.EntryFrame) line 1122
- listViewVehicleWaypoints_Click line ~1128 → LoadFrame(...)                 line 1138
- listViewEventWaypoints_Click   line ~1144 → LoadFrame(...)                 line 1154
- TryNavigateToMarker     line ~2085 → LoadFrame(waypoint.EntryFrame)        line 2100
- ProcessCmdKey arrow keys line 2287-2290 → LoadFrame(...)

From Forms/MainForm.cs panelTimeline guards (PRESERVE, do not modify):
- panelTimeline_MouseDown line 2112-2125  (guards at line 2115-2116)
- panelTimeline_MouseMove line 2127+      (guards already present)

From Services/VideoService.cs LoadVideoAsync (~line 138-153):
```csharp
currentVideoFile = filePath;                 // line 140
totalFrames = (int)videoCapture.Get(...);    // line 141
fps = videoCapture.Get(...);                 // line 142
currentFrameIndex = 0;                       // line 143
LoadFrame(0);                                // line 145  <-- REMOVE (duplicate, MainForm:302 will do it)
VideoLoaded?.Invoke(this, filePath);         // line 147
await LoadSubtitleForVideo(filePath);        // line 150
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce _isVideoReady gate + remove duplicate initial LoadFrame(0)</name>
  <files>Forms/MainForm.cs, Services/VideoService.cs</files>
  <action>
RELI-06 잔존 지연을 일으키는 cold-decoder blocking seek 을 근본 차단하기 위한 **ready-gate + 중복제거** 단계.

**1. Services/VideoService.cs: 중복 LoadFrame(0) 제거 (line 145)**

Remove line 145 `LoadFrame(0);` inside `LoadVideoAsync`. `MainForm.LoadVideoWithSubtitle` 가 line 302 에서 이미 `_videoService.LoadFrame(0)` 을 호출하므로 동일한 cold seek 가 두 번 발생하는 것을 막는다.

Target state of Services/VideoService.cs lines 140-150 (after edit):
```csharp
currentVideoFile = filePath;
totalFrames = (int)videoCapture.Get(VideoCaptureProperties.FrameCount);
fps = videoCapture.Get(VideoCaptureProperties.Fps);
currentFrameIndex = 0;

// NOTE: 첫 프레임 로드는 MainForm.LoadVideoWithSubtitle 에서 수행한다 (중복 seek 방지, RELI-06).
VideoLoaded?.Invoke(this, filePath);

// Attempt to load SRT subtitle
await LoadSubtitleForVideo(filePath);
```

**2. Forms/MainForm.cs: `_isVideoReady` + `_pendingAutoPlay` 필드 추가**

Add two private fields in the Fields region near line 49-55 (next to `selectedWaypoint`):
```csharp
// RELI-06 (Phase 5.5 gap closure): 첫 프레임 페인트 + LoadLabelingData 완료까지 false.
// 타임라인 외 모든 LoadFrame 경로에서 이 플래그로 early-return 한다.
private bool _isVideoReady;
// 자동 재생을 first-paint 이후로 연기하기 위한 one-shot 플래그.
private bool _pendingAutoPlay;
```

**3. Forms/MainForm.cs: LoadFrame 내부 belt-and-suspenders 가드 (line 360 뒤에 삽입)**

In `LoadFrame(int frameIndex)` (~line 356), **after** the existing `if (!_videoService.IsVideoLoaded) return;` line (360), insert the ready-check. Keep this silent per D-08:

```csharp
private void LoadFrame(int frameIndex)
{
    try
    {
        if (!_videoService.IsVideoLoaded) return;
        // RELI-06 (gap closure): cold-decoder 기간 LoadFrame 차단.
        // 첫 프레임이 페인트되기 전에는 어떠한 seek 도 수행하지 않는다.
        if (!_isVideoReady) return;
        if (frameIndex < 0 || frameIndex >= _videoService.TotalFrames) return;
        ...
    }
}
```

**4. Forms/MainForm.cs: LoadVideoWithSubtitle 흐름 개편 (line 280-322)**

- 로드 시작 직전에 `_isVideoReady = false;` 명시 설정
- 기존 자동 재생 호출(line 319-322)은 제거. 대신 `_pendingAutoPlay = true;` 설정
- 첫 프레임 LoadFrame(0) 호출 블록에서 **`_isVideoReady = true`를 설정한 뒤** `_videoService.LoadFrame(0)` 을 호출해야 하는지 검토: 아니다. 첫 프레임은 ready-gate 가 false 인 동안에도 렌더되어야 하므로 `_videoService.LoadFrame(0)` 직접 호출(현재 line 302) 을 유지하고 `MainForm.LoadFrame` 래퍼는 호출하지 않는다. 페인트 완료 후 `pictureBoxVideo_Paint` 핸들러에서 `_isVideoReady = true` 로 전이한다.

Target state of LoadVideoWithSubtitle (line 275-323 부근, 편집 후):
```csharp
// 로드 시작 — 가드 플래그 초기화 (RELI-06 gap closure)
_isVideoReady = false;
_pendingAutoPlay = false;

if (isPlaying)
{
    isPlaying = false;
    btnPlay.Text = "\u25B6";
    timerPlayback.Stop();
}

try
{
    await _videoService.LoadVideoAsync(filePath, token);
    token.ThrowIfCancellationRequested();

    if (!_videoService.IsVideoLoaded)
    {
        MessageBox.Show("Failed to open video file.", "Error", MessageBoxButtons.OK, MessageBoxIcon.Error);
        return;
    }

    _isDirty = false;

    // Show first frame (직접 VideoService 호출 — MainForm.LoadFrame 래퍼의 ready-gate 를 우회)
    var bitmap = _videoService.LoadFrame(0);
    if (bitmap != null)
    {
        pictureBoxVideo.Image?.Dispose();
        pictureBoxVideo.Image = bitmap;
    }

    UpdateTimeLabels();
    labelTitle.Text = $"ASLT(v1.0) - {Path.GetFileName(filePath)}";

    this.Focus();
    this.Activate();

    // Load JSON
    await LoadLabelingData(filePath);

    // RELI-06 (gap closure): 자동 재생을 first-paint handshake 로 연기.
    // _isVideoReady 는 pictureBoxVideo.Paint 에서 첫 프레임이 실제 페인트된 직후 true 로 전이된다.
    _pendingAutoPlay = true;
}
catch (OperationCanceledException) { ... }  // 기존 유지
catch (IOException ioEx) { ... }            // 기존 유지
catch (OpenCvSharp.OpenCVException ocvEx) { ... }  // 기존 유지
catch (Exception ex) { ... }                // 기존 유지
```

**5. Forms/MainForm.cs: pictureBoxVideo.Paint 에 first-paint handshake 추가**

pictureBoxVideo 에 이미 Paint 핸들러가 바인딩되어 있다면(박스 렌더링용) 해당 핸들러 **끝부분**에, 없다면 Designer/초기화 지점에서 새 람다를 추가한다. 아래 패턴을 사용:

```csharp
// RELI-06 (gap closure): 첫 프레임이 실제로 화면에 페인트된 직후 한 번만 ready 전이.
if (!_isVideoReady && _videoService.IsVideoLoaded && pictureBoxVideo.Image != null)
{
    _isVideoReady = true;

    if (_pendingAutoPlay)
    {
        _pendingAutoPlay = false;
        if (!isPlaying)
        {
            btnPlay_Click(null, EventArgs.Empty);
        }
    }
}
```

기존 pictureBoxVideo_Paint 핸들러가 있는 경우 해당 메서드 끝에 위 블록을 추가하라. grep 으로 확인: `grep -n "pictureBoxVideo_Paint\|pictureBoxVideo.Paint" Forms/MainForm.cs`

**Constraints:**
- D-08: 가드는 조용히 return. MessageBox/Log.Warning 금지. Log.Debug 한 줄 정도는 허용이나 권장되지 않음.
- 기존 timeline guard(panelTimeline_MouseDown/MouseMove, line 2112+) 는 손대지 않는다 (Phase 5.5-01 에서 이미 통과).
- USAB-08 drag-carry rebind 로직(LoadFrame line 371-425) 은 수정 금지.
- Nullable 비활성 환경. 기본값 false 면 충분.
  </action>
  <verify>
    <automated>dotnet build -c Debug ASLTv1.0.sln</automated>
    추가 grep 검증:
    - `grep -n "_isVideoReady" Forms/MainForm.cs` → 필드 선언 1회 + LoadFrame 가드 1회 + Paint handshake 1회 + 호출측 가드(Task 2에서 추가될 자리) 이상.
    - `grep -n "LoadFrame(0)" Services/VideoService.cs` → 0 match (제거됨).
    - `grep -n "btnPlay_Click(null" Forms/MainForm.cs` → LoadVideoWithSubtitle 내부에서는 사라지고 Paint handshake 안에만 남음.
  </verify>
  <done>
    - Services/VideoService.cs line 145 의 LoadFrame(0) 이 제거되었다.
    - MainForm 필드에 `_isVideoReady`, `_pendingAutoPlay` 가 추가되었다.
    - LoadFrame 함수가 `!_isVideoReady` 시 조용히 early-return 한다.
    - LoadVideoWithSubtitle 이 더 이상 즉시 btnPlay_Click 을 호출하지 않는다.
    - pictureBoxVideo.Paint 에서 첫 프레임 페인트 시 `_isVideoReady = true` 전이 및 pending auto-play 소비가 일어난다.
    - `dotnet build -c Debug` 0 errors, 0 new warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard all non-timeline LoadFrame callers (timer, keys, rewind/forward, waypoint clicks)</name>
  <files>Forms/MainForm.cs</files>
  <action>
Task 1 의 `_isVideoReady` 플래그를 **cold-decoder 구간에 발화하는 모든 경로의 진입부**에 early-return 가드로 배치한다. 각 경로 모두 D-08 에 따라 조용히 return.

**2.1 timerPlayback_Tick (line 774) — 최우선**
Interval=33ms 로 폭주하는 주범.
```csharp
private void timerPlayback_Tick(object sender, EventArgs e)
{
    // RELI-06 (gap closure): 첫 프레임 페인트 전 틱 무시 (cold-decoder 보호).
    if (!_isVideoReady) return;

    long currentTime = DateTime.Now.Ticks / 10000;
    long elapsedMs = currentTime - lastFrameTime;
    // ... (기존 코드 유지)
}
```

**2.2 ProcessCmdKey arrow keys (line 2282-2293)**
`isVideoLoaded` 체크 뒤에 `_isVideoReady` 추가.
```csharp
protected override bool ProcessCmdKey(ref Message msg, Keys keyData)
{
    bool isVideoLoaded = _videoService.IsVideoLoaded;
    // RELI-06 (gap closure): 로드 직후 화살표 seek 폭주 방지.
    if (isVideoLoaded && _isVideoReady)
    {
        if (keyData == (Keys.Shift | Keys.Left))  { LoadFrame(Math.Max(0, _videoService.CurrentFrameIndex - (int)(_videoService.Fps * 2))); return true; }
        if (keyData == (Keys.Shift | Keys.Right)) { LoadFrame(Math.Min(_videoService.TotalFrames - 1, _videoService.CurrentFrameIndex + (int)(_videoService.Fps * 2))); return true; }
        if (keyData == Keys.Left)  { LoadFrame(Math.Max(0, _videoService.CurrentFrameIndex - (int)(_videoService.Fps * 5))); return true; }
        if (keyData == Keys.Right) { LoadFrame(Math.Min(_videoService.TotalFrames - 1, _videoService.CurrentFrameIndex + (int)(_videoService.Fps * 5))); return true; }
    }
    return base.ProcessCmdKey(ref msg, keyData);
}
```
주의: `isVideoLoaded` 변수 자체의 선언은 유지 (다른 분기에서 사용되면 유지해야 회귀 없음). `_isVideoReady` 체크는 화살표 분기만 감싼다. `isVideoLoaded` 가 이 함수 내 다른 곳에서 쓰이지 않는 경우에도 기존 변수 유지(사소한 가독성 비용 < 회귀 위험).

**2.3 btnRewind_Click / btnForward_Click (line 797-809)**
```csharp
private void btnRewind_Click(object sender, EventArgs e)
{
    // RELI-06 (gap closure)
    if (!_isVideoReady) return;
    int framesToMove = (int)(_videoService.Fps * 5);
    int newFrame = Math.Max(0, _videoService.CurrentFrameIndex - framesToMove);
    LoadFrame(newFrame);
}

private void btnForward_Click(object sender, EventArgs e)
{
    // RELI-06 (gap closure)
    if (!_isVideoReady) return;
    int framesToMove = (int)(_videoService.Fps * 5);
    int newFrame = Math.Min(_videoService.TotalFrames - 1, _videoService.CurrentFrameIndex + framesToMove);
    LoadFrame(newFrame);
}
```

**2.4 listViewPersonWaypoints_Click / listViewVehicleWaypoints_Click / listViewEventWaypoints_Click (line ~1110, 1128, 1144)**

각 핸들러의 **함수 진입부**에 guard 추가. `suppressWaypointClickOnce` 체크보다 앞에 배치.
```csharp
private void listViewPersonWaypoints_Click(object sender, EventArgs e)
{
    // RELI-06 (gap closure)
    if (!_isVideoReady) return;
    if (suppressWaypointClickOnce) { suppressWaypointClickOnce = false; return; }
    // ... 기존 코드 유지
}
```
동일하게 listViewVehicleWaypoints_Click, listViewEventWaypoints_Click 에도 적용.

**2.5 TryNavigateToMarker (line ~2083-2110)**
이 메서드는 panelTimeline_MouseDown 에서 호출되며, panelTimeline_MouseDown 에는 이미 `!_videoService.IsVideoLoaded` 가드가 있다. 하지만 _isVideoReady 는 그보다 엄격하므로 panelTimeline_MouseDown 의 기존 가드 다음 줄에 추가 체크를 삽입해 통일성 확보:

```csharp
private void panelTimeline_MouseDown(object sender, MouseEventArgs e)
{
    // RELI-06: 영상 로드 완료 전 타임라인 입력 무시 (조용히 return, D-08)
    if (!_videoService.IsVideoLoaded) return;
    if (_videoService.TotalFrames == 0) return;
    // RELI-06 (gap closure): cold-decoder 기간 보호 — first paint 전 입력 차단
    if (!_isVideoReady) return;

    // ... 기존 코드 유지
}
```

panelTimeline_MouseMove 의 기존 가드 뒤에도 동일 패턴으로 `if (!_isVideoReady) return;` 한 줄 추가.

**Constraints:**
- D-08: 모든 가드 silent return. MessageBox 금지.
- USAB-08 (Person/Vehicle drag-carry rebind) 경로 — 드래그는 bbox/pictureBox 이벤트 기반이며 LoadFrame 을 직접 호출하지 않으므로 본 가드 영향 밖. 기존 LoadFrame 내부의 rebind 블록(line 371-425)은 완전히 그대로 둔다.
- FUNC-08, USAB-09 버튼 핸들러(btnEntry/btnExit) 는 수정 금지 — 이 버튼들은 LoadFrame 을 호출하나 사용자의 의도적 액션이며 로드 완료 후에만 의미가 있다. 필요 시 가드 추가는 v2 로 미룸 (현재 1fps 증상과 무관). Waypoint 이동 기능은 `_isVideoReady=true` 이후에만 사용되므로 실제 충돌 없음.
- 기존 `!_videoService.IsVideoLoaded` 가드는 그대로 유지하고 그 뒤에 `!_isVideoReady` 를 "추가" 하는 형태로만 변경.
  </action>
  <verify>
    <automated>dotnet build -c Debug ASLTv1.0.sln</automated>
    grep 검증:
    - `grep -cn "if (!_isVideoReady) return;" Forms/MainForm.cs` → ≥ 7 matches (LoadFrame + timerPlayback_Tick + btnRewind + btnForward + 3개 listView + panelTimeline 2개). ProcessCmdKey 는 `isVideoLoaded && _isVideoReady` 형태이므로 별도 매칭.
    - `grep -n "isVideoLoaded && _isVideoReady" Forms/MainForm.cs` → 1 match (ProcessCmdKey).
    - `grep -n "panelTimeline_MouseDown\|panelTimeline_MouseMove" Forms/MainForm.cs` → 기존 2개 핸들러 여전히 존재.

    수동 런타임 스모크 (build 성공 후 별도 확인):
    1. 앱 실행 → 영상 파일 선택.
    2. 로드 진행 중(화면이 첫 프레임으로 바뀌기 전) 즉시 Space/Left/Right/Rewind/Forward/Waypoint 클릭.
    3. 첫 프레임 페인트 후에야 재생 시작되며 1fps 지연이 관찰되지 않아야 한다.
  </verify>
  <done>
    - timerPlayback_Tick, btnRewind_Click, btnForward_Click, 3개 listViewWaypoints_Click, panelTimeline_MouseDown/Move 모두 `_isVideoReady` 가드 포함.
    - ProcessCmdKey 화살표 분기가 `_isVideoReady` 를 요구.
    - `dotnet build -c Debug` 0 errors.
    - 기존 panelTimeline 가드 로직은 보존됨 (D-07 회귀 없음).
    - 수동 재현 시 "초당 1프레임" 지연 현상이 사라진다 (UAT RELI-06 A 재검증 시 pass 예상).
  </done>
</task>

</tasks>

<verification>
1. Build: `dotnet build -c Debug ASLTv1.0.sln` → 0 errors, 0 new warnings.
2. Static grep checks (see per-task verify blocks).
3. UAT RELI-06 A 재실행:
   - 영상 파일 선택 직후 로드 진행 중 타임라인/화살표/Rewind/Forward/Waypoint 클릭 폭주 입력.
   - 기대: 입력 모두 무시, 첫 프레임 페인트 후 정상 재생 시작, "초당 1프레임" 지연 소멸.
4. UAT RELI-06 B 회귀:
   - 로드 완료 후 타임라인, 화살표, 버튼, Waypoint 리스트 모두 기존대로 동작.
5. USAB-08 회귀:
   - Person bbox 드래그 + 화살표로 프레임 이동 → drag-carry rebind 정상 (LoadFrame 내부 로직 무변경).
6. USAB-09 회귀:
   - Waypoint 선택 후 Entry/Exit 버튼 이동 기능 정상 (05.5-01 로직 무변경).
</verification>

<success_criteria>
- 초기 로드 중 ~1fps 지연이 재현되지 않는다 (UAT RELI-06 A pass).
- 로드 완료 후 모든 탐색 경로가 회귀 없이 동작 (UAT RELI-06 B, USAB-08, USAB-09 pass).
- 빌드 0 errors. 새 warning 없음.
- 중복 LoadFrame(0) 제거 확인 (grep).
- D-08 준수: 가드 경로에 MessageBox/Log.Warning 없음.
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-기능보정-안정화/05.5-02-SUMMARY.md` documenting:
- Fields added (`_isVideoReady`, `_pendingAutoPlay`)
- Guard call sites (list)
- LoadVideoAsync duplicate LoadFrame(0) removal
- Auto-play deferral via pictureBoxVideo.Paint handshake
- UAT RELI-06 A re-test result
</output>
