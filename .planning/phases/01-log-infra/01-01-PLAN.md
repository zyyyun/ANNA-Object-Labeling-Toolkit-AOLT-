---
phase: 01-log-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ASLTv1.0.csproj
  - Services/LogService.cs
  - Program.cs
  - Forms/MainForm.cs
  - Services/VideoService.cs
  - Services/JsonService.cs
autonomous: true
requirements:
  - MAINT-01
  - SECU-02
  - SECU-03

must_haves:
  truths:
    - "앱 시작 시 날짜별 로그 파일(AOLT-yyyy-MM-dd.log)이 logs/ 폴더에 생성된다"
    - "앱 시작/종료/JSON 저장/라이선스 오류 이벤트가 [AUDIT] 접두어로 Info 레벨에 기록된다"
    - "기존 Debug.WriteLine 8개소가 모두 Serilog 호출로 대체되어 로그 파일에 기록된다"
    - "로그에 MAC 주소 또는 사용자 식별 정보가 포함되지 않는다"
    - "30일 초과 로그 파일이 자동 삭제된다"
    - "로그 레벨(Debug/Info/Warning/Error)이 구분되어 기록된다"
  artifacts:
    - path: "Services/LogService.cs"
      provides: "Serilog 초기화, 감사 로그 메서드, 종료 처리"
      exports: ["Initialize()", "AuditAppStart()", "AuditAppStop()", "AuditJsonSave(string)", "AuditLicenseError(string)", "CloseAndFlush()"]
    - path: "ASLTv1.0.csproj"
      provides: "Serilog NuGet 패키지 참조"
      contains: "Serilog"
    - path: "Program.cs"
      provides: "앱 시작/종료 감사 로그 호출"
      contains: "LogService"
  key_links:
    - from: "Program.cs"
      to: "Services/LogService.cs"
      via: "LogService.Initialize() at app start, LogService.CloseAndFlush() at app end"
      pattern: "LogService\\.Initialize|LogService\\.CloseAndFlush"
    - from: "Forms/MainForm.cs"
      to: "Serilog.Log"
      via: "Log.Error / Log.Warning replacing Debug.WriteLine"
      pattern: "Log\\.(Error|Warning|Information|Debug)"
    - from: "Services/VideoService.cs"
      to: "Serilog.Log"
      via: "Log.Error replacing Debug.WriteLine"
      pattern: "Log\\.(Error|Warning)"
    - from: "Services/JsonService.cs"
      to: "Serilog.Log"
      via: "Log.Warning replacing Debug.WriteLine"
      pattern: "Log\\.(Warning|Error)"
---

<objective>
Serilog 기반 파일 로그 인프라를 구축하고, 기존 Debug.WriteLine 8개소를 모두 교체하며, 4개 감사 이벤트를 기록하는 로그 시스템을 완성한다.

Purpose: GS인증 유지보수성(MAINT-01) 및 보안성(SECU-02, SECU-03) 요구사항 충족 -- 구조화 로그와 감사 추적이 가능해야 한다.
Output: LogService.cs (Serilog 래퍼), 수정된 Program.cs/MainForm.cs/VideoService.cs/JsonService.cs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-log-infra/01-CONTEXT.md

@ASLTv1.0.csproj
@Program.cs
@Services/VideoService.cs
@Services/JsonService.cs
@Forms/MainForm.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serilog NuGet 추가 및 LogService 생성</name>
  <files>ASLTv1.0.csproj, Services/LogService.cs</files>
  <read_first>
    - ASLTv1.0.csproj (현재 패키지 참조 확인)
    - Services/VideoService.cs (기존 서비스 클래스 패턴 참조 -- region 구조, XML 주석 스타일)
    - Services/JsonService.cs (기존 서비스 클래스 패턴 참조)
  </read_first>
  <action>
1. ASLTv1.0.csproj에 Serilog NuGet 패키지 3개 추가 (기존 ItemGroup에 추가):
   ```xml
   <PackageReference Include="Serilog" Version="4.2.0" />
   <PackageReference Include="Serilog.Sinks.File" Version="6.0.0" />
   <PackageReference Include="Serilog.Sinks.Console" Version="6.0.0" />
   ```

2. Services/LogService.cs 생성 -- 정적 클래스로 구현 (per D-01, 기존 CoordinateHelper 정적 패턴 참조):
   - namespace: ASLTv1.Services
   - XML 주석(/// summary) 한국어로 작성 (기존 패턴)
   - #region 블록으로 구분: Initialization, Audit Events, Shutdown

   Initialize() 메서드:
   - 로그 경로: AppDomain.CurrentDomain.BaseDirectory 아래 "logs" 폴더 (실행 폴더/logs/)
   - Directory.CreateDirectory()로 logs 폴더 생성
   - Serilog Logger 구성 (코드 기반, per Claude's Discretion):
     ```csharp
     Log.Logger = new LoggerConfiguration()
         .MinimumLevel.Debug()                          // per D-03
         .WriteTo.Console(outputTemplate: LOG_TEMPLATE)
         .WriteTo.File(
             path: Path.Combine(logDir, "AOLT-.log"),   // per D-09: AOLT-2026-04-16.log
             rollingInterval: RollingInterval.Day,       // per D-09: 날짜별 로테이션
             retainedFileCountLimit: 30,                 // per D-08: 30일 보관
             outputTemplate: LOG_TEMPLATE,
             encoding: System.Text.Encoding.UTF8)
         .CreateLogger();
     ```
   - LOG_TEMPLATE 상수: "{Timestamp:yyyy-MM-dd HH:mm:ss.fff} [{Level:u3}] {Message:lj}{NewLine}{Exception}"

   감사 이벤트 메서드 4개 (per D-04, D-05 -- Info 레벨, [AUDIT] 접두어):
   - AuditAppStart(): Log.Information("[AUDIT] 애플리케이션 시작")
   - AuditAppStop(): Log.Information("[AUDIT] 애플리케이션 종료")
   - AuditJsonSave(string filePath): Log.Information("[AUDIT] JSON 저장: {FilePath}", filePath)
   - AuditLicenseError(string reason): Log.Information("[AUDIT] 라이선스 오류: {Reason}", reason)
   주의: 감사 로그 메서드에서 MAC 주소, 사용자 이름, IP 등 개인정보를 절대 파라미터로 받거나 기록하지 않음 (per D-06, D-07)

   CloseAndFlush() 메서드:
   - Log.CloseAndFlush() 호출

3. dotnet restore 실행하여 패키지 복원 확인
  </action>
  <verify>
    <automated>cd "C:/Users/ANNA/AOLTv1.0" && dotnet restore && dotnet build --no-restore -c Debug -p:Platform=x64 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ASLTv1.0.csproj contains: PackageReference Include="Serilog" Version="4.2.0"
    - ASLTv1.0.csproj contains: PackageReference Include="Serilog.Sinks.File" Version="6.0.0"
    - ASLTv1.0.csproj contains: PackageReference Include="Serilog.Sinks.Console" Version="6.0.0"
    - Services/LogService.cs exists and contains: namespace ASLTv1.Services
    - Services/LogService.cs contains: public static class LogService
    - Services/LogService.cs contains: public static void Initialize()
    - Services/LogService.cs contains: public static void AuditAppStart()
    - Services/LogService.cs contains: public static void AuditAppStop()
    - Services/LogService.cs contains: public static void AuditJsonSave(string filePath)
    - Services/LogService.cs contains: public static void AuditLicenseError(string reason)
    - Services/LogService.cs contains: public static void CloseAndFlush()
    - Services/LogService.cs contains: rollingInterval: RollingInterval.Day
    - Services/LogService.cs contains: retainedFileCountLimit: 30
    - Services/LogService.cs contains: [AUDIT]
    - Services/LogService.cs contains: "AOLT-.log"
    - Services/LogService.cs does NOT contain: MAC or MacAddress or GetPhysicalAddress
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Serilog 패키지가 설치되고, LogService.cs가 생성되어 빌드가 성공한다. Initialize()로 날짜별 로그 파일을 생성하고, 4개 감사 이벤트 메서드가 [AUDIT] 접두어로 Info 레벨에 기록한다.</done>
</task>

<task type="auto">
  <name>Task 2: Program.cs 감사 로그 연결 및 Debug.WriteLine 8개소 Serilog 교체</name>
  <files>Program.cs, Forms/MainForm.cs, Services/VideoService.cs, Services/JsonService.cs</files>
  <read_first>
    - Services/LogService.cs (Task 1에서 생성한 API 확인)
    - Program.cs (현재 진입점 코드)
    - Forms/MainForm.cs (Debug.WriteLine 5개소 위치: 328행, 434행, 504행, 607행, 673행)
    - Services/VideoService.cs (Debug.WriteLine 2개소 위치: 208행, 472행)
    - Services/JsonService.cs (Debug.WriteLine 1개소 위치: 207행)
  </read_first>
  <action>
1. Program.cs 수정 -- 앱 시작/종료 감사 로그 (per D-04):
   - using Serilog; 및 using ASLTv1.Services; 추가
   - Main() 메서드를 try-finally로 감싸기:
     ```csharp
     [STAThread]
     static void Main()
     {
         LogService.Initialize();
         LogService.AuditAppStart();
         try
         {
             ApplicationConfiguration.Initialize();
             Application.Run(new MainForm());
         }
         finally
         {
             LogService.AuditAppStop();
             LogService.CloseAndFlush();
         }
     }
     ```

2. Forms/MainForm.cs -- Debug.WriteLine 5개소를 Serilog 호출로 교체 (per D-02):
   - 파일 상단에 using Serilog; 추가
   - 328행 부근 (프레임 로드 오류):
     Debug.WriteLine($"[프레임 로드 오류] {ex.Message}\n{ex.StackTrace}")
     → Log.Error(ex, "[프레임 로드 오류] {Message}", ex.Message)
   - 434행 부근 (JSON 로드 오류):
     Debug.WriteLine($"[JSON 로드 오류] {ex.Message}\n{ex.StackTrace}")
     → Log.Error(ex, "[JSON 로드 오류] {Message}", ex.Message)
   - 504행 부근 (JSON 저장 버튼 오류):
     Debug.WriteLine($"[JSON 저장 버튼 오류] {outerEx.Message}\n{outerEx.StackTrace}")
     → Log.Error(outerEx, "[JSON 저장 버튼 오류] {Message}", outerEx.Message)
     추가: JSON 저장 성공 경로에 LogService.AuditJsonSave(파일경로) 호출 추가 (저장 성공 후, catch 블록 이전)
   - 607행 부근 (재생 버튼 오류):
     Debug.WriteLine($"[재생 버튼 오류] {ex.Message}")
     → Log.Error(ex, "[재생 버튼 오류] {Message}", ex.Message)
   - 673행 부근 (Exit 버튼 오류):
     Debug.WriteLine($"[Exit 버튼 오류] {ex.Message}")
     → Log.Error(ex, "[Exit 버튼 오류] {Message}", ex.Message)

3. Services/VideoService.cs -- Debug.WriteLine 2개소 교체 (per D-02):
   - 파일 상단에 using Serilog; 추가
   - using System.Diagnostics; 의 Debug.WriteLine 참조 제거 (다른 Debug 용도 없으면 using 자체 제거)
   - 208행 부근 (프레임 로드 오류):
     System.Diagnostics.Debug.WriteLine($"[프레임 로드 오류] {ex.Message}\n{ex.StackTrace}")
     → Log.Error(ex, "[프레임 로드 오류] {Message}", ex.Message)
   - 472행 부근 (자막 로드 오류):
     System.Diagnostics.Debug.WriteLine($"[자막 로드 오류] {ex.Message}")
     → Log.Warning("[자막 로드 오류] {Message}", ex.Message)

4. Services/JsonService.cs -- Debug.WriteLine 1개소 교체 (per D-02):
   - 파일 상단에 using Serilog; 추가
   - 207행 부근 (백업 파일 생성 실패):
     System.Diagnostics.Debug.WriteLine($"[JSON 로드] 백업 파일 생성 실패: {backupEx.Message}")
     → Log.Warning("[JSON 로드] 백업 파일 생성 실패: {Message}", backupEx.Message)

5. 기존 Debug.WriteLine이 완전히 제거되었는지 확인 (병행 유지 안 함, per D-02)
  </action>
  <verify>
    <automated>cd "C:/Users/ANNA/AOLTv1.0" && dotnet build -c Debug -p:Platform=x64 2>&1 | tail -5 && echo "--- Debug.WriteLine check ---" && grep -rn "Debug\.WriteLine" Forms/MainForm.cs Services/VideoService.cs Services/JsonService.cs 2>&1 || echo "No Debug.WriteLine remaining (PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - Program.cs contains: LogService.Initialize()
    - Program.cs contains: LogService.AuditAppStart()
    - Program.cs contains: LogService.AuditAppStop()
    - Program.cs contains: LogService.CloseAndFlush()
    - Program.cs contains: using ASLTv1.Services
    - Forms/MainForm.cs contains: using Serilog
    - Forms/MainForm.cs contains: Log.Error(
    - Forms/MainForm.cs contains: LogService.AuditJsonSave(
    - Forms/MainForm.cs does NOT contain: Debug.WriteLine
    - Services/VideoService.cs contains: using Serilog
    - Services/VideoService.cs contains: Log.Error(
    - Services/VideoService.cs contains: Log.Warning(
    - Services/VideoService.cs does NOT contain: Debug.WriteLine
    - Services/JsonService.cs contains: using Serilog
    - Services/JsonService.cs contains: Log.Warning(
    - Services/JsonService.cs does NOT contain: Debug.WriteLine
    - dotnet build succeeds with 0 errors
    - grep -rn "Debug.WriteLine" across all 4 files returns no matches
  </acceptance_criteria>
  <done>Program.cs에서 앱 시작/종료 감사 로그가 기록되고, Debug.WriteLine 8개소가 모두 Serilog Log.Error/Log.Warning으로 교체되며, JSON 저장 성공 시 감사 로그가 기록된다. 빌드 성공하고 Debug.WriteLine이 0개 남아있다.</done>
</task>

</tasks>

<verification>
1. dotnet build -c Debug -p:Platform=x64 성공 (빌드 오류 0건)
2. grep -rn "Debug\.WriteLine" Forms/ Services/ -- 결과 0건 (모두 교체 확인)
3. Services/LogService.cs에 Initialize, AuditAppStart, AuditAppStop, AuditJsonSave, AuditLicenseError, CloseAndFlush 6개 public 메서드 존재
4. grep -rn "MAC\|MacAddress\|GetPhysicalAddress" Services/LogService.cs -- 결과 0건 (개인정보 미포함)
5. Program.cs에 try-finally 구조로 AuditAppStart/AuditAppStop/CloseAndFlush 호출
</verification>

<success_criteria>
- Serilog 3개 패키지가 csproj에 등록되고 빌드가 성공한다
- LogService.cs가 Services/ 폴더에 존재하며 날짜별 로테이션(30일) 설정이 포함된다
- 감사 이벤트 4개(시작/종료/저장/라이선스오류)가 [AUDIT] 접두어로 기록 가능하다
- Debug.WriteLine 8개소가 모두 Serilog 호출로 교체되어 0개 남아있다
- 로그에 MAC 주소/사용자 식별 정보가 기록되지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/01-log-infra/01-01-SUMMARY.md`
</output>
